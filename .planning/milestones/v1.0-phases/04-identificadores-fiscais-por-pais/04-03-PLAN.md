---
phase: 04-identificadores-fiscais-por-pais
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/features/individual-signup/IndividualSignupPage.tsx
  - src/features/individual-signup/individualSignupReview.ts
  - src/features/company-signup/CompanySignupPage.tsx
  - src/features/company-signup/companySignupReview.ts
  - tests/country-signup-integration.test.ts
autonomous: true
requirements: [DOC-04, DOC-05]

must_haves:
  truths:
    - "Individual signup lets the user pick a country and shows the correct personal-ID field (CPF/CI/SSN) with the right label/mask/placeholder"
    - "Company signup lets the user pick a country and shows the correct company-ID field (CNPJ/RUC/EIN)"
    - "Country defaults to defaultCountryForLocale(active locale) on first render"
    - "Payload sends normalized digits via spec.normalize (toApiValue = normalize)"
    - "Review sections format the document per selected country, masking the personal ID"
  artifacts:
    - path: "src/features/individual-signup/IndividualSignupPage.tsx"
      provides: "CountrySelect + DocumentIdInput wired for personType=individual"
      contains: "DocumentIdInput"
    - path: "src/features/company-signup/CompanySignupPage.tsx"
      provides: "CountrySelect + DocumentIdInput wired for personType=company"
      contains: "DocumentIdInput"
    - path: "tests/country-signup-integration.test.ts"
      provides: "Payload normalization + review-formatting source assertions"
      contains: "describe"
  key_links:
    - from: "src/features/individual-signup/IndividualSignupPage.tsx"
      to: "src/components/ui/DocumentIdInput.tsx"
      via: "country-aware document field"
      pattern: "DocumentIdInput"
    - from: "src/features/individual-signup/individualSignupReview.ts"
      to: "src/lib/reviewDisplay.ts"
      via: "formatDocumentForReview(value, country, 'individual')"
      pattern: "formatDocumentForReview"
    - from: "src/features/company-signup/companySignupReview.ts"
      to: "src/lib/identifiers/countryIdentifiers.ts"
      via: "getIdentifierSpec(country,'company').normalize for payload"
      pattern: "getIdentifierSpec|normalize"
---

<objective>
Wire the 04-02 components and 04-01 registry into the individual and company signup flows: add a `CountrySelect`, swap `TaxIdInput` for `DocumentIdInput`, default the country from the active locale, send normalized digits in the payload, and format the review section per country (masking the personal ID).

Purpose: This is the user-visible payoff of the phase — signup selects a country and shows the correct document field, label, mask, validation, and review formatting for BR/PY/US.

Output: Updated `IndividualSignupPage`/`CompanySignupPage` + their review modules, plus an integration test asserting payload normalization and per-country review formatting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-identificadores-fiscais-por-pais/04-CONTEXT.md
@.planning/phases/04-identificadores-fiscais-por-pais/04-01-SUMMARY.md
@.planning/phases/04-identificadores-fiscais-por-pais/04-02-SUMMARY.md

<interfaces>
<!-- Components/registry from 04-01/04-02 — consume directly. -->
From src/components/ui/DocumentIdInput.tsx:
  DocumentIdInput (props: country: CountryCode, personType: PersonType, value: string, onChange: (v: string) => void, label?, id?, required?, error?, placeholder?)
From src/components/ui/CountrySelect.tsx:
  CountrySelect (props: value: CountryCode, onChange: (c: CountryCode) => void, label?, error?, id?)
From src/lib/identifiers/countryIdentifiers.ts:
  getIdentifierSpec(country, personType): IdentifierSpec   // .normalize, .labelKey, .validate
  defaultCountryForLocale(locale): CountryCode
  type CountryCode, PersonType
From src/lib/formatLocale.ts:
  getActiveLocale(): SupportedLocale
From src/lib/reviewDisplay.ts (generalized in 04-02):
  formatDocumentForReview(value, country, personType): string
From src/features/individual-signup/individualSignupReview.ts (to modify):
  type IndividualSignupFormValues { ...; taxId: string; } ; buildIndividualSignupPayload; buildIndividualSignupReviewSections
From src/features/company-signup/companySignupReview.ts (to modify):
  type CompanySignupFormValues { ...; taxId: string; } ; buildCompanySignupPayload; buildCompanySignupReviewSections
</interfaces>

<!-- NOTE: access-request (RequestAccessPage/requestAccessReview) stays on TaxIdInput + the legacy
     BR overloads of reviewDisplay — do NOT change it in this plan. It must keep working as-is. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire country selection into Individual signup (personType=individual)</name>
  <files>src/features/individual-signup/IndividualSignupPage.tsx, src/features/individual-signup/individualSignupReview.ts, tests/country-signup-integration.test.ts</files>
  <read_first>
    - src/features/individual-signup/IndividualSignupPage.tsx (state `taxId`, `TaxIdInput kind="CPF"`, formValues memo, submit flow)
    - src/features/individual-signup/individualSignupReview.ts (IndividualSignupFormValues, buildIndividualSignupPayload uses toTaxIdApiValue, buildIndividualSignupReviewSections uses formatDocumentForReview(taxId,'CPF'))
    - src/components/ui/DocumentIdInput.tsx and src/components/ui/CountrySelect.tsx (04-02)
    - src/lib/identifiers/countryIdentifiers.ts (getIdentifierSpec, defaultCountryForLocale)
    - src/lib/formatLocale.ts (getActiveLocale)
    - src/lib/reviewDisplay.ts (generalized formatDocumentForReview)
  </read_first>
  <action>
    In `individualSignupReview.ts`: add a `country: CountryCode` field to `IndividualSignupFormValues`. In `buildIndividualSignupPayload`, replace `taxId: toTaxIdApiValue(values.taxId)` with `taxId: getIdentifierSpec(values.country, 'individual').normalize(values.taxId)` (normalized digits = API value per D locked decision). In `buildIndividualSignupReviewSections`, replace the hardcoded CPF field with a dynamic one: label from `getIdentifierSpec(values.country,'individual').code` (or its translated `labelKey`) and value `formatDocumentForReview(values.taxId, values.country, 'individual')`. Import `getIdentifierSpec`/`CountryCode` from `../../lib/identifiers`. Keep the rest of the review sections and `INDIVIDUAL_SIGNUP_REVIEW_COPY` intact.

    In `IndividualSignupPage.tsx`: add `const [country, setCountry] = useState<CountryCode>(() => defaultCountryForLocale(getActiveLocale()))`. When the country changes, clear the `taxId` state so a stale mask from another country cannot persist. Add the `country` field to the `formValues` memo and its dependency array. Render a `CountrySelect` (label "País" / t) above the document field, bound to `country`/`setCountry`. Replace `<TaxIdInput kind="CPF" label="CPF" ... />` with `<DocumentIdInput country={country} personType="individual" label={<translated spec label>} value={taxId} onChange={setTaxId} required />`, deriving the label from `getIdentifierSpec(country,'individual')`. Do not otherwise restructure the form or submit flow.

    Create `tests/country-signup-integration.test.ts` with a `describe('individual signup')` block. Because tests run under the Node runner (no DOM), assert on the pure review-module functions: e.g. `buildIndividualSignupPayload({...country:'US', taxId:'123-45-6789'}).taxId === '123456789'`; `buildIndividualSignupPayload({...country:'PY', taxId:'1.234.567'}).taxId === '1234567'`; and that a US review section value equals `'***-**-6789'` and a BR review section value equals `'529.***.***-25'` for taxId `'529.982.247-25'`. Also assert (source-level, via readFileSync of `IndividualSignupPage.tsx`) that the page imports `DocumentIdInput`, `CountrySelect`, and initializes country via `defaultCountryForLocale`.
  </action>
  <verify>
    <automated>npx tsx --test tests/country-signup-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `IndividualSignupFormValues` has a `country: CountryCode` field; payload `taxId` is `getIdentifierSpec(country,'individual').normalize(taxId)` (digits only).
    - `IndividualSignupPage.tsx` renders `CountrySelect` + `DocumentIdInput personType="individual"`, defaults country via `defaultCountryForLocale(getActiveLocale())`, and clears taxId on country change; no `TaxIdInput` remains in this page.
    - Review section value uses `formatDocumentForReview(taxId, country, 'individual')` (US -> `***-**-6789`, BR -> `529.***.***-25`).
    - `npx tsx --test tests/country-signup-integration.test.ts` individual block passes.
  </acceptance_criteria>
  <done>Individual signup selects country, shows the correct personal-ID field, sends normalized digits, and formats/masks the review per country; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire country selection into Company signup (personType=company)</name>
  <files>src/features/company-signup/CompanySignupPage.tsx, src/features/company-signup/companySignupReview.ts, tests/country-signup-integration.test.ts</files>
  <read_first>
    - src/features/company-signup/CompanySignupPage.tsx (state `taxId`, `TaxIdInput kind="CNPJ"`, formValues memo, submit flow)
    - src/features/company-signup/companySignupReview.ts (CompanySignupFormValues, buildCompanySignupPayload uses toTaxIdApiValue, buildCompanySignupReviewSections uses formatDocumentForReview(taxId,'CNPJ'))
    - src/features/individual-signup/individualSignupReview.ts (mirror the country-field pattern established in Task 1)
    - src/components/ui/DocumentIdInput.tsx, src/components/ui/CountrySelect.tsx
    - src/lib/identifiers/countryIdentifiers.ts, src/lib/formatLocale.ts, src/lib/reviewDisplay.ts
  </read_first>
  <action>
    Apply the same pattern as Task 1 with `personType='company'`. In `companySignupReview.ts`: add `country: CountryCode` to `CompanySignupFormValues`; payload `taxId: getIdentifierSpec(values.country,'company').normalize(values.taxId)`; review "CNPJ" field becomes dynamic — label from `getIdentifierSpec(values.country,'company')` (CNPJ/RUC/EIN), value `formatDocumentForReview(values.taxId, values.country, 'company')` (company docs render in full, no masking). Keep the authorization/terms sections and `COMPANY_SIGNUP_REVIEW_COPY` intact.

    In `CompanySignupPage.tsx`: add `const [country, setCountry] = useState<CountryCode>(() => defaultCountryForLocale(getActiveLocale()))`; clear `taxId` on country change; add `country` to the formValues memo + deps; render `CountrySelect` above the document field; replace `<TaxIdInput kind="CNPJ" ... />` with `<DocumentIdInput country={country} personType="company" label={<translated spec label>} value={taxId} onChange={setTaxId} required />`. Leave the rest of the company form (name, admin, authorization checkbox) unchanged.

    Extend `tests/country-signup-integration.test.ts` with a `describe('company signup')` block: `buildCompanySignupPayload({...country:'US', taxId:'12-3456789'}).taxId === '123456789'`; `buildCompanySignupPayload({...country:'PY', taxId:'80017726-6'}).taxId === '800177266'`; a US review value equals `'12-3456789'` and a PY review value equals `'80017726-6'` (company = full, unmasked); a BR review value equals `'11.222.333/0001-81'` for taxId `'11222333000181'`. Add source-level assertions that `CompanySignupPage.tsx` imports `DocumentIdInput`/`CountrySelect` and uses `defaultCountryForLocale`.
  </action>
  <verify>
    <automated>npx tsx --test tests/country-signup-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `CompanySignupFormValues` has a `country: CountryCode` field; payload `taxId` is `getIdentifierSpec(country,'company').normalize(taxId)` (digits only).
    - `CompanySignupPage.tsx` renders `CountrySelect` + `DocumentIdInput personType="company"`, defaults country via `defaultCountryForLocale(getActiveLocale())`, clears taxId on country change; no `TaxIdInput` remains in this page.
    - Company review value uses `formatDocumentForReview(taxId, country, 'company')` and renders full/unmasked (US `12-3456789`, PY `80017726-6`, BR `11.222.333/0001-81`).
    - `npx tsx --test tests/country-signup-integration.test.ts` passes fully (individual + company blocks).
  </acceptance_criteria>
  <done>Company signup selects country, shows the correct company-ID field, sends normalized digits, and formats the review per country; full integration test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signup form → API payload | User-entered document + country normalized to digits client-side before submit; backend (doqyn-auth-service) is the authoritative validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing/Tampering | non-BR document acceptance at backend | transfer | Backend validation/storage of non-BR docs lives in doqyn-auth-service (cross-repo, DEFERRED per CONTEXT). This plan ships only the client layer; if auth-service rejects non-CPF/CNPJ, that is a tracked cross-repo follow-up, not a blocker. |
| T-04-06 | Information Disclosure | review dialog personal ID | mitigate | Individual doc masked via formatDocumentForReview before render; company doc shown in full by design. |
</threat_model>

<verification>
- `npx tsx --test tests/country-signup-integration.test.ts` green (individual + company payload normalization + per-country review formatting).
- `npx tsc -p tsconfig.app.json --noEmit` shows no new errors in the four modified signup files.
- Manual/source check: neither signup page still references `TaxIdInput`; access-request page still does (unchanged).
</verification>

<success_criteria>
Individual and company signup select a country (defaulting from the active locale), display the correct document field/label/mask/validation for BR/PY/US, send normalized digits in the payload, and render per-country review formatting with personal-ID masking — with access-request left untouched.
</success_criteria>

<output>
Create `.planning/phases/04-identificadores-fiscais-por-pais/04-03-SUMMARY.md` when done.
Stage ONLY this plan's files. Do NOT `git add -A` (working tree carries pre-existing feat/document-chat changes/failures — ~22 unrelated test failures and ~3 TS6133 are baseline and out of scope).
</output>
