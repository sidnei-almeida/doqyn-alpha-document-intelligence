---
phase: 03-formata-o-sens-vel-a-locale
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/features/document-send/utils/workflowLogHelpers.ts
  - src/features/document-send/utils/historyFormat.ts
  - src/features/document-send/DocumentSendPage.tsx
  - src/features/document-send/hooks/useBulkUploadQueue.ts
  - src/features/document-send/services/analyzePdf.ts
  - src/features/document-send/services/processDocumentWithAI.ts
  - src/features/sharing/components/ShareDocumentModal.tsx
  - src/features/document-update-version/utils/documentMetadataDisplay.ts
  - tests/format-locale-migration-send.test.ts
autonomous: true
requirements: [FMT-01, FMT-03]
must_haves:
  truths:
    - "Every document-send, sharing, and document-update-version formatting site uses the central formatLocale functions instead of hardcoded 'pt-BR'"
    - "pt-BR rendered output for these sites is unchanged (same Intl options preserved)"
    - "documentMetadataDisplay date-only formatting keeps timeZone:'UTC' and follows the active locale"
  artifacts:
    - path: "tests/format-locale-migration-send.test.ts"
      provides: "Guard asserting each migrated file imports formatLocale and drops hardcoded pt-BR formatting"
      contains: "formatLocale"
  key_links:
    - from: "src/features/document-send/utils/historyFormat.ts"
      to: "src/lib/formatLocale.ts"
      via: "formatDate + formatTime import"
      pattern: "@/lib/formatLocale"
    - from: "src/features/sharing/components/ShareDocumentModal.tsx"
      to: "src/lib/formatLocale.ts"
      via: "formatDate + formatDateTime import"
      pattern: "@/lib/formatLocale"
    - from: "src/features/document-update-version/utils/documentMetadataDisplay.ts"
      to: "src/lib/formatLocale.ts"
      via: "formatDate with timeZone UTC"
      pattern: "@/lib/formatLocale"
---

<objective>
Migrate all hardcoded `'pt-BR'` date/time formatting in the document-send feature, the sharing ShareDocumentModal, and document-update-version metadata display to the central `formatLocale` functions from plan 03-01, preserving each call site's exact Intl options so pt-BR output is unchanged (FMT-01 date/time, FMT-03 migration).

Purpose: These call sites now follow the active locale while staying byte-identical in pt-BR.
Output: 8 migrated files + a migration guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-formata-o-sens-vel-a-locale/03-CONTEXT.md

<interfaces>
From src/lib/formatLocale.ts (created in 03-01):
- `formatDate(value, opts?, locale?)` — date; no opts delegates to native toLocaleDateString(locale).
- `formatDateTime(value, opts?, locale?)` — date+time; no opts delegates to native toLocaleString(locale).
- `formatTime(value, opts?, locale?)` — time; no opts delegates to native toLocaleTimeString(locale).
All default to the active locale when `locale` is omitted. Frontend imports via `@/lib/formatLocale` (no .js extension, src convention).

BASELINE: working tree has pre-existing unrelated failures (~22 tests, ~3 TS6133) from feat/document-chat — out of scope. Only ensure the files you change are clean and the new migration test passes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate document-send formatting sites</name>
  <files>src/features/document-send/utils/workflowLogHelpers.ts, src/features/document-send/utils/historyFormat.ts, src/features/document-send/DocumentSendPage.tsx, src/features/document-send/hooks/useBulkUploadQueue.ts, src/features/document-send/services/analyzePdf.ts, src/features/document-send/services/processDocumentWithAI.ts</files>
  <read_first>
    - src/lib/formatLocale.ts (target functions)
    - src/features/document-send/utils/historyFormat.ts (formatHistoryDate: toLocaleDateString {day numeric, month short, year numeric} + toLocaleTimeString {hour/minute 2-digit}; keep the ` às ` join; leave formatFileSizeLabel untouched)
    - src/features/document-send/utils/workflowLogHelpers.ts (formatWorkflowTimestamp: toLocaleTimeString {hour/minute/second 2-digit})
    - src/features/document-send/DocumentSendPage.tsx line ~76, src/features/document-send/hooks/useBulkUploadQueue.ts line ~75, src/features/document-send/services/analyzePdf.ts line ~208 (identical pattern: `${now.toLocaleDateString('pt-BR')} ${now.toLocaleTimeString('pt-BR', { hour:'2-digit', minute:'2-digit' })}`)
    - src/features/document-send/services/processDocumentWithAI.ts line ~55 (new Date().toLocaleDateString('pt-BR'), no options)
  </read_first>
  <action>
    In each file add `import { ... } from '@/lib/formatLocale'` (only the functions used per file) and replace the native calls, preserving options exactly:
    - historyFormat.ts: `value.toLocaleDateString('pt-BR', { day:'numeric', month:'short', year:'numeric' })` becomes `formatDate(value, { day:'numeric', month:'short', year:'numeric' })`; `value.toLocaleTimeString('pt-BR', { hour:'2-digit', minute:'2-digit' })` becomes `formatTime(value, { hour:'2-digit', minute:'2-digit' })`. Keep the returned template with ` às `. Do not touch formatFileSizeLabel.
    - workflowLogHelpers.ts: `date.toLocaleTimeString('pt-BR', { hour:'2-digit', minute:'2-digit', second:'2-digit' })` becomes `formatTime(date, { hour:'2-digit', minute:'2-digit', second:'2-digit' })`. Leave the rest of the file (id/duration helpers) unchanged.
    - DocumentSendPage.tsx / useBulkUploadQueue.ts / analyzePdf.ts: replace the interpolated line with `${formatDate(now)} ${formatTime(now, { hour:'2-digit', minute:'2-digit' })}` (formatDate with no options reproduces the bare toLocaleDateString default byte-identically).
    - processDocumentWithAI.ts: `new Date().toLocaleDateString('pt-BR')` becomes `formatDate(new Date())`.
    Do not introduce fenced code. Keep Prettier style (single quotes, semicolons, printWidth 100). These are frontend src modules — use the `@/lib/formatLocale` alias and no .js extension.
  </action>
  <acceptance_criteria>
    - None of these six files contain a hardcoded `'pt-BR'` in a toLocale*/Intl call after migration.
    - historyFormat.ts uses formatDate({day:'numeric',month:'short',year:'numeric'}) and formatTime({hour:'2-digit',minute:'2-digit'}) and still joins with ' às '.
    - workflowLogHelpers.ts formatWorkflowTimestamp uses formatTime with {hour,minute,second:'2-digit'}.
    - The three interpolated-line sites use formatDate(now) + formatTime(now,{hour:'2-digit',minute:'2-digit'}).
    - processDocumentWithAI uses formatDate(new Date()).
    - Each modified file imports from '@/lib/formatLocale'. formatFileSizeLabel and unrelated helpers unchanged.
  </acceptance_criteria>
  <verify>
    <automated>! grep -RIl "toLocaleDateString('pt-BR'\|toLocaleTimeString('pt-BR'\|toLocaleString('pt-BR'" src/features/document-send</automated>
  </verify>
  <done>All six document-send sites route through formatLocale with identical options; no hardcoded pt-BR remains in the feature folder's formatting calls.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ShareDocumentModal and documentMetadataDisplay</name>
  <files>src/features/sharing/components/ShareDocumentModal.tsx, src/features/document-update-version/utils/documentMetadataDisplay.ts</files>
  <read_first>
    - src/lib/formatLocale.ts (formatDate, formatDateTime)
    - src/features/sharing/components/ShareDocumentModal.tsx lines ~130-145 (expiresAt: toLocaleDateString('pt-BR') no options; lastAccessAt: toLocaleString('pt-BR') no options)
    - src/features/document-update-version/utils/documentMetadataDisplay.ts lines ~43-55 (module-level DATE_ONLY = Intl.DateTimeFormat('pt-BR', { day:'2-digit', month:'2-digit', year:'numeric', timeZone:'UTC' }) reused inside formatDateOnly)
  </read_first>
  <action>
    ShareDocumentModal.tsx: add `import { formatDate, formatDateTime } from '@/lib/formatLocale'`. Replace `new Date(share.expiresAt).toLocaleDateString('pt-BR')` with `formatDate(new Date(share.expiresAt))` and `new Date(share.lastAccessAt).toLocaleString('pt-BR')` with `formatDateTime(new Date(share.lastAccessAt))`. Both are no-options calls; formatDate/formatDateTime delegate to native toLocaleDateString/toLocaleString with the active locale, preserving the pt-BR default byte-for-byte (including the time portion for lastAccessAt).
    documentMetadataDisplay.ts: add `import { formatDate } from '@/lib/formatLocale'`. Remove the module-level `DATE_ONLY` constant (it can no longer be cached because the locale changes at runtime) and change `formatDateOnly` so its final return calls `formatDate(d, { day:'2-digit', month:'2-digit', year:'numeric', timeZone:'UTC' })` at call time. Keep the existing null/empty/NaN guards exactly as they are.
  </action>
  <acceptance_criteria>
    - ShareDocumentModal.tsx no longer contains `toLocaleDateString('pt-BR')` or `toLocaleString('pt-BR')`; it imports and uses formatDate + formatDateTime.
    - documentMetadataDisplay.ts no longer declares a module-level Intl.DateTimeFormat('pt-BR', ...); formatDateOnly calls formatDate with { day:'2-digit', month:'2-digit', year:'numeric', timeZone:'UTC' } and keeps its guards.
    - Both files import from '@/lib/formatLocale'.
  </acceptance_criteria>
  <verify>
    <automated>! grep -RIn "toLocale\(Date\|Time\|\)String('pt-BR'\|Intl.DateTimeFormat('pt-BR'" src/features/sharing/components/ShareDocumentModal.tsx src/features/document-update-version/utils/documentMetadataDisplay.ts</automated>
  </verify>
  <done>Both files formatted via formatLocale with preserved options (including UTC timeZone for date-only); no hardcoded pt-BR remains.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration guard test for this cluster</name>
  <files>tests/format-locale-migration-send.test.ts</files>
  <read_first>
    - tests/dashboard-layout.test.ts (source-string assertion style with readFileSync + node:test)
    - the 8 files migrated in Tasks 1-2 (to assert their post-migration shape)
  </read_first>
  <action>
    Create `tests/format-locale-migration-send.test.ts` following the dashboard-layout.test.ts pattern (readFileSync of each source under src/, node:test describe/it, node:assert/strict). For each of the 8 migrated files assert: (a) the source includes `@/lib/formatLocale`, and (b) the source does NOT include the hardcoded formatting substrings `toLocaleDateString('pt-BR'`, `toLocaleTimeString('pt-BR'`, `toLocaleString('pt-BR'`, or `Intl.DateTimeFormat('pt-BR'`. Use assert.ok(source.includes('@/lib/formatLocale')) and assert.equal(source.includes("toLocaleString('pt-BR'"), false) etc. Do not assert on files outside this plan's files_modified (do not touch mock-data.ts, which legitimately keeps the data value language: 'pt-BR').
  </action>
  <acceptance_criteria>
    - tests/format-locale-migration-send.test.ts exists and runs under `npx tsx --test`.
    - It asserts import of '@/lib/formatLocale' and absence of hardcoded pt-BR formatting for all 8 migrated files.
    - `npx tsx --test tests/format-locale-migration-send.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test tests/format-locale-migration-send.test.ts</automated>
  </verify>
  <done>Migration guard passes, proving every document-send/sharing/metadata site uses formatLocale and dropped hardcoded pt-BR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document dates → formatters | Date strings from documents/shares are formatted for display; no execution or injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Share lastAccessAt/expiresAt display | accept | Same data already displayed pre-migration; formatting change is locale-only, no new exposure |
| T-03-04 | Tampering | Active locale drives output | mitigate | Locale resolved via getActiveLocale (validated) from plan 03-01 |
</threat_model>

<verification>
- `npx tsx --test tests/format-locale-migration-send.test.ts` passes.
- No `toLocale*('pt-BR'` or `Intl.DateTimeFormat('pt-BR'` in the 8 migrated files.
- pt-BR display output unchanged (options preserved at every site).
</verification>

<success_criteria>
- All 8 document-send/sharing/metadata sites use central formatLocale functions.
- Exact Intl options preserved (byte-identical pt-BR, including UTC date-only and datetime defaults).
- Migration guard test passes; mock-data.ts data value untouched.
</success_criteria>

<output>
Create `.planning/phases/03-formata-o-sens-vel-a-locale/03-02-SUMMARY.md` when done.
</output>
