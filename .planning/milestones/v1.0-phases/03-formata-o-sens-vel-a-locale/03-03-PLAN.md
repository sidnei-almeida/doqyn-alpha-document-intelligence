---
phase: 03-formata-o-sens-vel-a-locale
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/features/library/utils/sortDocuments.ts
  - src/features/signature/components/SignaturesAssignedPanel.tsx
  - src/features/signature/utils/signatureSummaryDisplay.ts
  - src/features/signature/SignaturePortalPage.tsx
  - src/features/signature/InternalSignaturePage.tsx
  - src/features/external-share/ExternalSharePortalPage.tsx
  - tests/format-locale-migration-signature.test.ts
autonomous: true
requirements: [FMT-01, FMT-02, FMT-03]
must_haves:
  truths:
    - "sortDocuments' four localeCompare calls use localeCompareActive (active locale) instead of hardcoded 'pt-BR'"
    - "Signature and external-share date displays use central formatDate/formatDateTime with preserved Intl options"
    - "pt-BR sort order and rendered dates are unchanged"
  artifacts:
    - path: "tests/format-locale-migration-signature.test.ts"
      provides: "Guard asserting each migrated file imports formatLocale and drops hardcoded pt-BR formatting/compare"
      contains: "formatLocale"
  key_links:
    - from: "src/features/library/utils/sortDocuments.ts"
      to: "src/lib/formatLocale.ts"
      via: "localeCompareActive import"
      pattern: "localeCompareActive"
    - from: "src/features/signature/SignaturePortalPage.tsx"
      to: "src/lib/formatLocale.ts"
      via: "formatDateTime import"
      pattern: "@/lib/formatLocale"
    - from: "src/features/external-share/ExternalSharePortalPage.tsx"
      to: "src/lib/formatLocale.ts"
      via: "formatDateTime import"
      pattern: "@/lib/formatLocale"
---

<objective>
Migrate the remaining hardcoded `'pt-BR'` usages — the four `localeCompare` calls in library sortDocuments plus the signature and external-share date displays — to the central `formatLocale` functions from plan 03-01, preserving options and sort semantics so pt-BR output is unchanged (FMT-01 date/time, FMT-02 numbers/sort compare, FMT-03 migration).

Purpose: Complete the migration set; sorting and portal date displays now follow the active locale while staying byte-identical in pt-BR.
Output: 6 migrated files + a migration guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-formata-o-sens-vel-a-locale/03-CONTEXT.md

<interfaces>
From src/lib/formatLocale.ts (created in 03-01):
- `formatDate(value, opts?, locale?)`, `formatDateTime(value, opts?, locale?)` — default to active locale.
- `localeCompareActive(a: string, b: string, locale?): number` — `a.localeCompare(b, locale ?? getActiveLocale())`.
Frontend imports via `@/lib/formatLocale` (no .js extension, src convention).

Discretion note (CONTEXT): sortDocuments may read the active locale (no locale param needed). Public portals (Signature/ExternalShare) reading the active locale from the singleton is acceptable behavior.

BASELINE: working tree has pre-existing unrelated failures (~22 tests, ~3 TS6133) from feat/document-chat — out of scope. Only ensure the files you change are clean and the new migration test passes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate library sortDocuments localeCompare (x4)</name>
  <files>src/features/library/utils/sortDocuments.ts</files>
  <read_first>
    - src/lib/formatLocale.ts (localeCompareActive signature)
    - src/features/library/utils/sortDocuments.ts (four `.localeCompare(..., 'pt-BR')` sites: name, status, owner, category; keep the `factor *` multiply and the updatedAt numeric branch untouched)
  </read_first>
  <action>
    Add `import { localeCompareActive } from '@/lib/formatLocale'`. Replace each of the four `X.localeCompare(Y, 'pt-BR')` expressions with `localeCompareActive(X, Y)`:
    - name: `localeCompareActive(documentDisplayName(a), documentDisplayName(b))`
    - status: `localeCompareActive(a.status ?? '', b.status ?? '')`
    - owner: `localeCompareActive(documentOwnerName(a), documentOwnerName(b))`
    - category: `localeCompareActive(a.categoryName ?? '', b.categoryName ?? '')`
    Keep the surrounding `factor *` and the `updatedAt`/default numeric-time branch exactly as they are. Do not add a locale parameter to sortDocuments (it reads the active locale at call time per CONTEXT discretion). Frontend src convention: `@/lib/formatLocale`, no .js extension.
  </action>
  <acceptance_criteria>
    - sortDocuments.ts contains no `localeCompare(` with a hardcoded 'pt-BR' argument.
    - All four sort branches call localeCompareActive with the same operands and keep the factor multiply.
    - The updatedAt/default numeric branch is unchanged; sortDocuments signature is unchanged.
    - File imports localeCompareActive from '@/lib/formatLocale'.
  </acceptance_criteria>
  <verify>
    <automated>! grep -n "localeCompare(.*'pt-BR'" src/features/library/utils/sortDocuments.ts && grep -qc "localeCompareActive" src/features/library/utils/sortDocuments.ts</automated>
  </verify>
  <done>All four sort comparisons use localeCompareActive; pt-BR ordering preserved; no hardcoded locale.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate signature and external-share date displays</name>
  <files>src/features/signature/components/SignaturesAssignedPanel.tsx, src/features/signature/utils/signatureSummaryDisplay.ts, src/features/signature/SignaturePortalPage.tsx, src/features/signature/InternalSignaturePage.tsx, src/features/external-share/ExternalSharePortalPage.tsx</files>
  <read_first>
    - src/lib/formatLocale.ts (formatDate, formatDateTime)
    - src/features/signature/components/SignaturesAssignedPanel.tsx (formatDate: keeps `if (!iso) return '—'`; toLocaleString {day/month/year/hour/minute '2-digit'})
    - src/features/signature/utils/signatureSummaryDisplay.ts (~line 124: toLocaleString {day/month/year '2-digit'} — date only)
    - src/features/signature/SignaturePortalPage.tsx (~line 33), src/features/signature/InternalSignaturePage.tsx (~line 32), src/features/external-share/ExternalSharePortalPage.tsx (~line 32) — all module-scope formatDate/formatShareDate helpers: toLocaleString {day/month/year/hour/minute '2-digit'}
  </read_first>
  <action>
    In each file add `import { ... } from '@/lib/formatLocale'` (formatDateTime, and formatDate where date-only) and replace, preserving options:
    - SignaturesAssignedPanel.tsx: keep the `if (!iso) return '—'` guard; replace `new Date(iso).toLocaleString('pt-BR', { day:'2-digit', month:'2-digit', year:'numeric', hour:'2-digit', minute:'2-digit' })` with `formatDateTime(new Date(iso), { day:'2-digit', month:'2-digit', year:'numeric', hour:'2-digit', minute:'2-digit' })`.
    - signatureSummaryDisplay.ts: replace `new Date(normalized.latestSignedAt).toLocaleString('pt-BR', { day:'2-digit', month:'2-digit', year:'numeric' })` with `formatDate(new Date(normalized.latestSignedAt), { day:'2-digit', month:'2-digit', year:'numeric' })` (date-only options; formatDate with explicit options is byte-identical to the prior toLocaleString with the same options).
    - SignaturePortalPage.tsx, InternalSignaturePage.tsx, ExternalSharePortalPage.tsx: replace `new Date(iso).toLocaleString('pt-BR', { day:'2-digit', month:'2-digit', year:'numeric', hour:'2-digit', minute:'2-digit' })` with `formatDateTime(new Date(iso), { day:'2-digit', month:'2-digit', year:'numeric', hour:'2-digit', minute:'2-digit' })` inside each module-scope helper. Keep the helper functions module-scope (reading the active locale at call time is acceptable per CONTEXT discretion for public portals). Do not restructure into the component body or the hook.
    Frontend src convention: `@/lib/formatLocale`, no .js extension. Keep Prettier style. No fenced code.
  </action>
  <acceptance_criteria>
    - None of the five files contain `toLocaleString('pt-BR'` after migration.
    - SignaturesAssignedPanel keeps its `'—'` guard and uses formatDateTime with the 5-field option set.
    - signatureSummaryDisplay uses formatDate with the date-only 3-field option set.
    - The three portal pages use formatDateTime with the 5-field option set in their module-scope helpers.
    - Each file imports from '@/lib/formatLocale'.
  </acceptance_criteria>
  <verify>
    <automated>! grep -RIn "toLocaleString('pt-BR'\|toLocaleDateString('pt-BR'\|toLocaleTimeString('pt-BR'" src/features/signature src/features/external-share</automated>
  </verify>
  <done>All five signature/external-share sites use formatLocale with preserved options; no hardcoded pt-BR remains.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration guard test for this cluster</name>
  <files>tests/format-locale-migration-signature.test.ts</files>
  <read_first>
    - tests/dashboard-layout.test.ts (source-string assertion style)
    - the 6 files migrated in Tasks 1-2
  </read_first>
  <action>
    Create `tests/format-locale-migration-signature.test.ts` following the dashboard-layout.test.ts pattern (readFileSync + node:test + node:assert/strict). For each of the 6 migrated files assert: (a) source includes `@/lib/formatLocale`, and (b) source does NOT include `toLocaleString('pt-BR'`, `toLocaleDateString('pt-BR'`, or `toLocaleTimeString('pt-BR'`. For sortDocuments.ts additionally assert source includes `localeCompareActive` and does NOT include `localeCompare(` followed by `'pt-BR'` (assert.equal(source.includes("localeCompare(") && source.includes("'pt-BR'"), false) is too broad — instead assert.equal(source.includes("'pt-BR')"), false)). Do not assert on files outside this plan's files_modified.
  </action>
  <acceptance_criteria>
    - tests/format-locale-migration-signature.test.ts exists and runs under `npx tsx --test`.
    - It asserts formatLocale import and absence of hardcoded pt-BR formatting for all 6 files, plus localeCompareActive presence in sortDocuments.ts.
    - `npx tsx --test tests/format-locale-migration-signature.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test tests/format-locale-migration-signature.test.ts</automated>
  </verify>
  <done>Migration guard passes, proving every signature/external-share/library-sort site uses formatLocale and dropped hardcoded pt-BR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signature/share dates → public portals | Dates displayed on public signature/external-share portals; formatting only, active locale read from singleton |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Public portal date display | accept | Same dates already shown pre-migration; change is locale formatting only |
| T-03-06 | Tampering | Active locale drives sort/format | mitigate | Locale resolved via getActiveLocale (validated) from plan 03-01 |
</threat_model>

<verification>
- `npx tsx --test tests/format-locale-migration-signature.test.ts` passes.
- No `toLocale*('pt-BR'` or `localeCompare(..., 'pt-BR')` in the 6 migrated files.
- pt-BR sort order and rendered dates unchanged (options and operands preserved).
</verification>

<success_criteria>
- sortDocuments uses localeCompareActive for all four keys; signature/external-share dates use central formatDate/formatDateTime.
- Exact Intl options and sort operands preserved (byte-identical pt-BR).
- Migration guard test passes.
</success_criteria>

<output>
Create `.planning/phases/03-formata-o-sens-vel-a-locale/03-03-SUMMARY.md` when done.
</output>
