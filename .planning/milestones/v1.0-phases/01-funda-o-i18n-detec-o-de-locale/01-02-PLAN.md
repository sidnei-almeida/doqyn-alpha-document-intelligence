---
phase: 01-funda-o-i18n-detec-o-de-locale
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/i18n/locales/pt-BR/common.json
  - src/i18n/locales/pt-BR/nav.json
  - src/i18n/locales/es-PY/common.json
  - src/i18n/locales/es-PY/nav.json
  - src/i18n/locales/en-US/common.json
  - src/i18n/locales/en-US/nav.json
  - src/i18n/index.ts
  - tests/i18n-catalogs.test.ts
autonomous: true
requirements: [I18N-02, I18N-03]
user_setup: []

must_haves:
  truths:
    - "Catalogs exist for pt-BR, es-PY and en-US, split into `common` and `nav` namespaces."
    - "pt-BR is complete for every key the shell/nav will consume; es-PY and en-US cover the same keys."
    - "i18next initializes once with these resources, fallbackLng pt-BR, and initial language from resolveSupportedLocale(navigator.languages)."
    - "Missing keys fall back to pt-BR rather than showing the raw key."
  artifacts:
    - path: "src/i18n/index.ts"
      provides: "Configured i18next singleton (initReactI18next, resources, fallbackLng pt-BR, namespaces)"
      contains: "initReactI18next"
    - path: "src/i18n/locales/pt-BR/nav.json"
      provides: "Portuguese nav strings (source of truth)"
      contains: "library"
    - path: "src/i18n/locales/es-PY/nav.json"
      provides: "Spanish (PY) nav strings"
    - path: "src/i18n/locales/en-US/nav.json"
      provides: "English (US) nav strings"
  key_links:
    - from: "src/i18n/index.ts"
      to: "src/i18n/config.ts"
      via: "import of resolveSupportedLocale, SUPPORTED_LOCALES, DEFAULT_LOCALE"
      pattern: "resolveSupportedLocale"
    - from: "src/i18n/index.ts"
      to: "src/i18n/locales"
      via: "JSON namespace imports composed into resources"
      pattern: "locales/.*\\.json"
---

<objective>
Author the message catalogs for the three locales and initialize the i18next runtime against them.

Purpose: Provide the translation resources and a single configured i18next instance so the provider (plan 01-03) can wrap the app and the shell can call `t()`. Fixes the resource/init half of I18N-01 and delivers I18N-02, wiring the tested detection helper into init (I18N-03).
Output: `common`/`nav` JSON catalogs for pt-BR/es-PY/en-US, `src/i18n/index.ts` initializing i18next with pt-BR fallback, and a catalog-parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-funda-o-i18n-detec-o-de-locale/01-CONTEXT.md
@./CLAUDE.md
@.planning/phases/01-funda-o-i18n-detec-o-de-locale/01-01-SUMMARY.md

<interfaces>
From src/i18n/config.ts (created in plan 01-01):
- `SUPPORTED_LOCALES` — readonly ['pt-BR', 'es-PY', 'en-US']
- `DEFAULT_LOCALE` — 'pt-BR'
- `type SupportedLocale`
- `resolveSupportedLocale(languages: readonly string[]): SupportedLocale`
</interfaces>

<facts>
- Catalog structure (locked): `src/i18n/locales/<locale>/<namespace>.json`. Namespaces this phase: `common`, `nav`.
- Missing-key fallback (locked): pt-BR.
- `resolveJsonModule` was enabled in `tsconfig.app.json` by plan 01-01, so JSON imports type-check.
- Shell/nav strings that will be migrated (plan 01-03) and therefore need keys — pt-BR source values taken from current code:
  - nav namespace: library="Biblioteca", sharedWithMe="Compartilhados comigo", toSign="Para assinar", recent="Recentes", favorites="Favoritos", trash="Lixeira", overview="Visão Geral", rules="Regras", users="Usuários", audit="Auditoria", tracking="Tracking", deactivated="Desativados", settings="Configurações", administration="Administração"
  - common namespace: help="Ajuda", settings="Configurações", storage="Armazenamento", manage="Gerenciar", expandSidebar="Expandir barra lateral", collapseSidebar="Recolher barra lateral", userMenu="Menu do usuário", account="Conta"
- react-i18next init pattern: `i18next.use(initReactI18next).init({...})`; expose the configured instance as the module default export / named `i18n`.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author common + nav catalogs for pt-BR, es-PY, en-US</name>
  <files>src/i18n/locales/pt-BR/common.json, src/i18n/locales/pt-BR/nav.json, src/i18n/locales/es-PY/common.json, src/i18n/locales/es-PY/nav.json, src/i18n/locales/en-US/common.json, src/i18n/locales/en-US/nav.json</files>
  <read_first>
    - src/lib/constants.ts (nav item labels — source of truth for pt-BR strings)
    - src/components/layout/WorkspaceTopBar.tsx (aria-labels "Ajuda", "Configurações")
    - src/components/layout/SidebarStoragePanel.tsx ("Armazenamento", "Gerenciar")
    - src/components/layout/Sidebar.tsx ("Administração", "Biblioteca", collapse/expand aria-labels)
  </read_first>
  <action>
    Create six JSON files under `src/i18n/locales/<locale>/{common,nav}.json` for locales `pt-BR`, `es-PY`, `en-US`. Use flat keys (no nesting) matching the key list in `<facts>`. pt-BR values are the exact existing Portuguese strings (source of truth). Provide accurate Spanish (Paraguay) translations for es-PY and US English for en-US covering the SAME key set in all three locales — every key present in pt-BR must exist in es-PY and en-US so parity holds. Keep JSON valid (double quotes, no trailing commas), Prettier-friendly. Do not invent keys the shell will not use; keep the set to what plan 01-03 migrates.
  </action>
  <verify>
    <automated>node -e "for(const l of ['pt-BR','es-PY','en-US'])for(const n of ['common','nav'])require('./src/i18n/locales/'+l+'/'+n+'.json');console.log('parseOK')"</automated>
  </verify>
  <acceptance_criteria>
    - All six JSON files exist and parse as valid JSON.
    - `nav.json` in every locale contains all 14 nav keys; `common.json` contains all 8 common keys.
    - pt-BR values equal the existing source strings (e.g. nav.library === "Biblioteca", nav.overview === "Visão Geral", common.manage === "Gerenciar").
    - Key sets are identical across the three locales for each namespace.
  </acceptance_criteria>
  <done>Catalogs exist for all three locales with matching keys and pt-BR anchored to current UI strings.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize i18next against the catalogs</name>
  <files>src/i18n/index.ts</files>
  <read_first>
    - src/i18n/config.ts (SUPPORTED_LOCALES, DEFAULT_LOCALE, resolveSupportedLocale)
    - CLAUDE.md (import type / verbatimModuleSyntax rules)
    - one catalog file, e.g. src/i18n/locales/pt-BR/nav.json (confirm shape)
  </read_first>
  <action>
    Create `src/i18n/index.ts` that imports the six JSON catalogs, composes a `resources` map keyed by locale then namespace (`common`, `nav`), and calls `i18next.use(initReactI18next).init({...})` with: `resources`; `lng` computed from `resolveSupportedLocale([...navigator.languages])` (guard for environments without `navigator` by falling back to `DEFAULT_LOCALE`) — this is the detection wiring for I18N-03; `fallbackLng: 'pt-BR'` and `defaultNS: 'common'`; `ns: ['common', 'nav']`; `supportedLngs` from `SUPPORTED_LOCALES`; `interpolation: { escapeValue: false }` (React escapes); and `returnNull: false`. Export the configured instance (default export named `i18n`). Ensure init runs exactly once (module singleton). Registering `i18next-browser-languagedetector` is optional and permitted, but the initial language MUST be resolved through `resolveSupportedLocale` so the locked mapping (es-*→es-PY, en-*→en-US, else→pt-BR) governs detection. Do NOT read/write localStorage (Phase 2). Do NOT wrap the app here — the provider is plan 01-03.
  </action>
  <verify>
    <automated>grep -q "initReactI18next" src/i18n/index.ts && grep -q "fallbackLng" src/i18n/index.ts && grep -q "resolveSupportedLocale" src/i18n/index.ts && npm run build --silent 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/i18n/index.ts` calls `i18next.use(initReactI18next).init(...)` with `resources` for all three locales and both namespaces.
    - `fallbackLng` is `'pt-BR'`; initial `lng` derives from `resolveSupportedLocale`.
    - Module exports a single configured i18next instance.
    - `npm run build` (which runs `tsc -b`) succeeds — JSON imports type-check.
  </acceptance_criteria>
  <done>A single configured i18next instance loads the three-locale catalogs with pt-BR fallback and detection-driven initial language.</done>
</task>

<task type="auto">
  <name>Task 3: Catalog parity + fallback test</name>
  <files>tests/i18n-catalogs.test.ts</files>
  <read_first>
    - tests/dashboard-layout.test.ts (node:test structure + reading files from ../src)
    - src/i18n/locales/pt-BR/nav.json (reference key set)
  </read_first>
  <action>
    Create `tests/i18n-catalogs.test.ts` using `node:test` + `node:assert/strict`. Load each `{common,nav}.json` for the three locales (read from `../src/i18n/locales/...`). Assert: (1) every namespace has an identical set of keys across pt-BR, es-PY, en-US; (2) no value is an empty string in any locale; (3) pt-BR anchor values match current UI (assert a few, e.g. nav.library === "Biblioteca", nav.overview === "Visão Geral"). This guards the I18N-02 parity/fallback contract without a DOM.
  </action>
  <verify>
    <automated>npx tsx --test tests/i18n-catalogs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test passes and fails loudly if a locale is missing a key or has an empty value.
    - `npm test` full suite passes.
  </acceptance_criteria>
  <done>Catalog parity and pt-BR anchoring are enforced by an automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog JSON → rendered UI | Translation values become UI text; React escapes interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Catalog values rendered via t() | accept | Values are project-authored static JSON, not user input; React escapes output and interpolation is `escapeValue:false` only because React handles escaping. |
| T-02-02 | Information Disclosure | Missing-key behavior | mitigate | `fallbackLng: 'pt-BR'` + parity test prevent raw keys/blank strings leaking to users. |
</threat_model>

<verification>
- `node -e` parses all six catalogs.
- `npx tsx --test tests/i18n-catalogs.test.ts` passes.
- `npm run build`, `npm run lint`, `npm test` pass.
</verification>

<success_criteria>
- Catalogs for pt-BR/es-PY/en-US in `common`/`nav` namespaces with pt-BR fallback (I18N-02).
- i18next initialized once with detection-driven initial language and pt-BR fallback (I18N-01 resources/init half, I18N-03 wiring).
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-i18n-detec-o-de-locale/01-02-SUMMARY.md` when done.
</output>
