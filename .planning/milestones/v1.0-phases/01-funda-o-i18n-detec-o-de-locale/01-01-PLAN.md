---
phase: 01-funda-o-i18n-detec-o-de-locale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.app.json
  - src/i18n/config.ts
  - tests/i18n-locale-detection.test.ts
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03]
user_setup: []

must_haves:
  truths:
    - "react-i18next, i18next and i18next-browser-languagedetector are declared dependencies and install cleanly."
    - "A pure function maps navigator language tags to one of the three supported locales with pt-BR as fallback."
    - "SUPPORTED_LOCALES and DEFAULT_LOCALE are exported constants usable by the i18next init."
    - "A unit test proves the detection mapping for es/en/pt/unknown/empty inputs without touching the DOM."
  artifacts:
    - path: "src/i18n/config.ts"
      provides: "SUPPORTED_LOCALES, DEFAULT_LOCALE, SupportedLocale type, resolveSupportedLocale() detection helper"
      exports: ["SUPPORTED_LOCALES", "DEFAULT_LOCALE", "SupportedLocale", "resolveSupportedLocale"]
    - path: "tests/i18n-locale-detection.test.ts"
      provides: "Unit coverage of the detection mapping"
      contains: "resolveSupportedLocale"
    - path: "package.json"
      provides: "i18n runtime dependencies"
      contains: "react-i18next"
  key_links:
    - from: "tests/i18n-locale-detection.test.ts"
      to: "src/i18n/config.ts"
      via: "import of resolveSupportedLocale"
      pattern: "resolveSupportedLocale"
---

<objective>
Establish the i18n runtime dependencies and the tested, DOM-free locale-detection core.

Purpose: Every later i18n step (catalogs, i18next init, provider, shell migration) depends on the supported-locale constants and the browser-language mapping. Getting these right and tested first prevents a scavenger hunt downstream and locks the detection contract (I18N-03).
Output: Installed i18n packages, `src/i18n/config.ts` (constants + `resolveSupportedLocale`), a passing unit test, and `resolveJsonModule` enabled so later JSON catalog imports type-check under `tsc -b`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-i18n-detec-o-de-locale/01-CONTEXT.md
@./CLAUDE.md

<facts>
- No i18n framework exists today. `src/i18n/` does not exist yet.
- Runtime is locked by user decision: react-i18next + i18next; i18next-browser-languagedetector permitted.
- Supported locales (locked): pt-BR (fallback/default), es-PY, en-US.
- Detection mapping (locked): es / es-* → es-PY; en / en-* → en-US; pt / pt-* / anything else → pt-BR.
- Tests are flat under `tests/*.test.ts`, run with `npm test` (`tsx --test tests/**/*.test.ts`) using the Node built-in test runner (`node:test`, `node:assert/strict`) — NOT Jest/Vitest. See `tests/dashboard-layout.test.ts` for the import style.
- Frontend uses alias `@` → `src`. TypeScript strict + `verbatimModuleSyntax` (use `import type`). Prettier: semicolons, single quotes, printWidth 100.
- `tsconfig.app.json` currently has NO `resolveJsonModule`; JSON namespace imports in later plans need it for `tsc -b` (part of `npm run build`).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supply-chain note (pre-approved)</name>
  <what-built>Supply-chain verification pre-approved by the operator: `react-i18next`, `i18next`, and `i18next-browser-languagedetector` are the official, de-facto-standard i18next libraries (millions of weekly downloads, official repos). No action beyond proceeding with install in Task 2.</what-built>
  <action>No-op. Verification recorded; proceed to install in Task 2.</action>
  <acceptance_criteria>
    - This task requires no code change; it documents that the package supply-chain gate was cleared before install.
  </acceptance_criteria>
  <done>Package legitimacy acknowledged; install may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Install i18n deps, add config constants + detection helper, enable JSON imports</name>
  <files>package.json, package-lock.json, tsconfig.app.json, src/i18n/config.ts</files>
  <read_first>
    - package.json (confirm no existing i18n deps; observe dependency ordering/style)
    - tsconfig.app.json (compilerOptions block where resolveJsonModule is added)
    - CLAUDE.md (import + type-only conventions for src/)
  </read_first>
  <action>
    Run `npm install react-i18next i18next i18next-browser-languagedetector` so the three packages land in `dependencies` and `package-lock.json` updates (satisfies the runtime half of I18N-01).
    Add `"resolveJsonModule": true` to `compilerOptions` in `tsconfig.app.json` so the JSON catalogs created in plan 01-02 type-check under `tsc -b`.
    Create `src/i18n/config.ts` exporting: `SUPPORTED_LOCALES` as a readonly tuple `['pt-BR', 'es-PY', 'en-US']`; `DEFAULT_LOCALE` = `'pt-BR'`; a `SupportedLocale` union type derived from `SUPPORTED_LOCALES` (use `import type` where applicable); and `resolveSupportedLocale(languages: readonly string[]): SupportedLocale`. The function maps by primary subtag of the FIRST provided tag whose primary subtag is `es`, `en`, or `pt`, case-insensitively: `es` → `'es-PY'`, `en` → `'en-US'`, `pt` → `'pt-BR'`, and any unrecognized/empty input → `DEFAULT_LOCALE` (`'pt-BR'`). Keep it pure and DOM-free (no `navigator`, no `document`) — callers pass the language list. This is the sole owner of the locked detection mapping for I18N-03.
    Do NOT create `index.ts` or catalogs here (plan 01-02 owns those). Do NOT add a language-selector UI (Phase 2). Do NOT read from localStorage (persistence is Phase 2).
  </action>
  <verify>
    <automated>npm ls react-i18next i18next i18next-browser-languagedetector >/dev/null 2>&1 && grep -q '"resolveJsonModule": true' tsconfig.app.json && grep -q 'resolveSupportedLocale' src/i18n/config.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `react-i18next`, `i18next`, `i18next-browser-languagedetector` under dependencies and `npm ls` resolves them.
    - `tsconfig.app.json` `compilerOptions` contains `"resolveJsonModule": true`.
    - `src/i18n/config.ts` exports `SUPPORTED_LOCALES`, `DEFAULT_LOCALE`, `SupportedLocale`, and `resolveSupportedLocale`.
    - `resolveSupportedLocale` references no `navigator`/`document` symbols (grep returns nothing for `navigator`/`document` in `src/i18n/config.ts`).
  </acceptance_criteria>
  <done>Deps installed, JSON imports enabled, and the detection helper + locale constants exist and export cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit-test the locale detection mapping</name>
  <files>tests/i18n-locale-detection.test.ts</files>
  <read_first>
    - tests/dashboard-layout.test.ts (canonical node:test structure: `import { describe, it } from 'node:test'`, `import assert from 'node:assert/strict'`)
    - src/i18n/config.ts (the function under test)
  </read_first>
  <behavior>
    - `['es-AR']` → `'es-PY'`; `['es']` → `'es-PY'`
    - `['en-GB']` → `'en-US'`; `['en']` → `'en-US'`
    - `['pt-PT']` → `'pt-BR'`; `['pt-BR']` → `'pt-BR'`
    - `['fr-FR']` → `'pt-BR'` (unknown language falls back)
    - `[]` → `'pt-BR'` (empty list falls back)
    - `['en-US', 'pt-BR']` → `'en-US'` (first preference wins)
    - Case-insensitivity: `['ES-py']` → `'es-PY'`
  </behavior>
  <action>
    Create `tests/i18n-locale-detection.test.ts` using `node:test` + `node:assert/strict`, importing `resolveSupportedLocale` from `src/i18n/config.ts` via a relative path (tests do not use the `@` alias — see existing tests resolving `../src`). Assert every case listed in `<behavior>`. Keep it pure/DOM-free.
  </action>
  <verify>
    <automated>npx tsx --test tests/i18n-locale-detection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test tests/i18n-locale-detection.test.ts` exits 0 with all assertions passing.
    - Test imports `resolveSupportedLocale` from the config module and covers es/en/pt/unknown/empty/precedence/case cases.
    - `npm test` (full suite) still passes.
  </acceptance_criteria>
  <done>Detection mapping is proven correct by a passing DOM-free unit test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | New third-party packages enter the supply chain during install |
| browser locale → app | `navigator.languages` is attacker-influenceable but only selects a UI language (no privilege) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install of react-i18next / i18next / i18next-browser-languagedetector | mitigate | Blocking human-verify checkpoint (Task 1) confirms each package on npmjs.com before install; packages treated as [ASSUMED] absent a RESEARCH audit table. |
| T-01-01 | Tampering | `resolveSupportedLocale` input | accept | Input only chooses a UI language; unknown values fall back to pt-BR. No trust decision made from it. |
</threat_model>

<verification>
- `npm ls react-i18next i18next i18next-browser-languagedetector` resolves all three.
- `npx tsx --test tests/i18n-locale-detection.test.ts` passes.
- `npm run lint` and `npm test` pass.
</verification>

<success_criteria>
- i18n runtime packages installed (I18N-01 runtime half).
- `resolveSupportedLocale` implements the locked es/en/pt/fallback mapping (I18N-03) and is unit-tested.
- Locale constants exported for downstream init/catalogs (I18N-02 foundation).
- JSON imports enabled for downstream catalogs.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-i18n-detec-o-de-locale/01-01-SUMMARY.md` when done.
</output>
