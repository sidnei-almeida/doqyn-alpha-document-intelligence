---
phase: 05-telefone-por-pais-integracao-no-cadastro
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/ui/PhoneInput.tsx
  - src/lib/reviewDisplay.ts
  - src/features/individual-signup/individualSignupReview.ts
  - src/features/company-signup/companySignupReview.ts
  - src/features/individual-signup/IndividualSignupPage.tsx
  - src/features/company-signup/CompanySignupPage.tsx
  - tests/phone-signup-integration.test.ts
autonomous: true
requirements: [TEL-01, TEL-02]

must_haves:
  truths:
    - "Both signup forms render a country/DDI selector next to the phone field, defaulting to the active locale's country."
    - "Selecting a country reformats the current phone value to that country's mask."
    - "The individual/company signup payload sends whatsapp as E.164 including the country dial code."
    - "For a BR phone the payload whatsapp equals '5554999999999' (byte-identical to today)."
    - "Review sections show the phone formatted per country (+55 54 99999-9999 / +595 981 234 567 / +1 (202) 555-0123)."
  artifacts:
    - path: "src/components/ui/PhoneInput.tsx"
      provides: "Country-aware phone input (DDI selector + formatted input, E.164-ready value)"
      contains: "PhoneInput"
      min_lines: 30
    - path: "tests/phone-signup-integration.test.ts"
      provides: "Payload/review coverage for BR/PY/US signup phone"
      contains: "595981234567"
  key_links:
    - from: "src/components/ui/PhoneInput.tsx"
      to: "src/lib/identifiers/phone.ts"
      via: "formatPhone/isCompletePhone via useFormattedInput"
      pattern: "formatPhone|isCompletePhone"
    - from: "src/features/individual-signup/individualSignupReview.ts"
      to: "src/lib/identifiers/phone.ts"
      via: "toE164 in buildIndividualSignupPayload"
      pattern: "toE164"
    - from: "src/features/individual-signup/IndividualSignupPage.tsx"
      to: "src/components/ui/PhoneInput.tsx"
      via: "JSX usage replacing WhatsappInput"
      pattern: "PhoneInput"
---

<objective>
Build the `PhoneInput` component (DDI/country selector + formatted input) and wire it into both signup pages so phone entry is country-aware (BR/PY/US), the payload carries E.164 with the country dial code, and the review dialog shows the phone formatted per country. BR behavior stays byte-identical.

Purpose: Delivers the user-facing half of TEL-01 and all of TEL-02 (country selector with locale-default). `WhatsappInput` and its other four callers (share/signature/invite/access-request) stay BR and untouched.
Output: `PhoneInput.tsx`; a country-aware overload on `reviewDisplay.formatPhone`; updated signup review/payload builders + pages; integration tests for BR/PY/US.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-CONTEXT.md
@.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-01-SUMMARY.md

<interfaces>
<!-- Contracts from plan 05-01 (phone.ts) and existing components. -->

From src/lib/identifiers/phone.ts (plan 05-01):
- `formatPhone(rawInput: string, country: CountryCode): string`
- `toE164(rawInput: string, country: CountryCode): string`   // dial-code + national, NO '+'
- `isCompletePhone(value: string, country: CountryCode): boolean`
- `defaultPhoneCountry(locale: SupportedLocale): CountryCode`
- `PHONE_COUNTRIES: Record<CountryCode, { dialCode; placeholder; nationalLengths; format }>`

From src/hooks/useFormattedInput.ts:
- `useFormattedInput({ value, onChange, format }) => { value, onChange, onPaste }`  // wire onto <Input>

From src/components/ui/WhatsappInput.tsx (the analog to mirror):
- uses useCallback handlers, `type="tel"` `inputMode="tel"` `autoComplete="tel"`, shows an incomplete error, forwards ...props to <Input>.

From src/components/ui/CountrySelect.tsx:
- `CountrySelect({ value: CountryCode, onChange: (c: CountryCode) => void, label?, id? })` — localized country names.

From src/lib/formatLocale.ts:
- `getActiveLocale(): SupportedLocale`

Existing signup review builders (to modify):
- `IndividualSignupFormValues` / `CompanySignupFormValues` have `whatsapp: string`, `country: CountryCode` (the tax-id country).
- `buildIndividualSignupPayload` / `buildCompanySignupPayload` currently do `whatsapp: toWhatsappApiValue(values.whatsapp)`.
- Review sections currently do `formatPhone(values.whatsapp)` (from src/lib/reviewDisplay.ts).

IMPORTANT back-compat: `reviewDisplay.formatPhone(value)` (single-arg) is ALSO called by access-request and invite review builders — that single-arg form MUST keep working (legacy formatWhatsapp). `toWhatsappApiValue` MUST stay exported/unchanged (access-request + invite still use it).

Convention: src/ imports use `@/` alias or extension-less relative paths; NO .js extension.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PhoneInput component (DDI selector + formatted input)</name>
  <read_first>
    - src/components/ui/WhatsappInput.tsx (structure to mirror: handlers, Input props, incomplete error)
    - src/hooks/useFormattedInput.ts (format/onChange/onPaste wiring)
    - src/components/ui/CountrySelect.tsx (DDI selector to reuse)
    - src/components/ui/Input.tsx analog via WhatsappInput usage (InputProps passthrough)
    - src/lib/identifiers/phone.ts (formatPhone/isCompletePhone/PHONE_COUNTRIES)
  </read_first>
  <behavior>
    - Renders a country selector + a formatted phone input.
    - Typing formats via formatPhone(raw, country); changing country reformats the current value to the new country's mask.
    - Shows 'Telefone incompleto.' when non-empty and !isCompletePhone, unless optional.
  </behavior>
  <action>
Create `src/components/ui/PhoneInput.tsx`. Define `PhoneInputProps extends Omit<InputProps, 'value' | 'onChange' | 'type'>` with: `value: string`, `onChange: (value: string) => void`, `country: CountryCode`, `onCountryChange: (country: CountryCode) => void`, `optional?: boolean`.

Compose a `CountrySelect` (reuse it as the DDI selector — Claude's Discretion allows a dial-code dropdown instead, but reusing CountrySelect gives localized names for free) and an `<Input>`. Use `useFormattedInput({ value, onChange, format })` where `format = useCallback((raw) => formatPhone(raw, country), [country])`. On the Input set `type="tel"`, `inputMode="tel"`, `autoComplete="tel"`, and `placeholder={props.placeholder ?? PHONE_COUNTRIES[country].placeholder}`.

When the country selector changes, call `onCountryChange(next)` AND reformat the existing value: `onChange(formatPhone(value, next))` so the displayed mask switches to the new country immediately.

Compute `incomplete = !optional && value.length > 0 && !isCompletePhone(value, country)` and pass `error={error ?? (incomplete ? 'Telefone incompleto.' : undefined)}` to the Input. Forward remaining `...props` (id, label, required) to the Input. Do NOT modify WhatsappInput.tsx.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "PhoneInput" || echo "no PhoneInput type errors"</automated>
  </verify>
  <acceptance_criteria>
    - PhoneInput exports a React component accepting value/onChange/country/onCountryChange/optional plus Input passthrough props.
    - With country='PY' the input placeholder is '+595 981 234 567'; with country='US' it is '+1 (202) 555-0123'; with country='BR' it is '+55 54 99999-9999' (from PHONE_COUNTRIES).
    - Changing country invokes both onCountryChange(next) and onChange(formatPhone(value, next)).
    - WhatsappInput.tsx is unchanged.
  </acceptance_criteria>
  <done>PhoneInput compiles, reuses phone.ts + useFormattedInput + CountrySelect, and reformats on country change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Country-aware review formatPhone + wire PhoneInput into both signup flows</name>
  <read_first>
    - src/lib/reviewDisplay.ts (formatPhone at lines 103-107; keep single-arg legacy behavior)
    - src/features/individual-signup/individualSignupReview.ts (form type, buildIndividualSignupPayload, review sections)
    - src/features/company-signup/companySignupReview.ts (form type, buildCompanySignupPayload, review sections)
    - src/features/individual-signup/IndividualSignupPage.tsx (whatsapp state, country state, WhatsappInput usage lines 182-188, formValues useMemo)
    - src/features/company-signup/CompanySignupPage.tsx (whatsapp state, WhatsappInput usage lines 212-218, formValues useMemo)
    - tests/country-signup-integration.test.ts (existing test — must stay green; forms omit any phone-country field)
  </read_first>
  <behavior>
    - reviewDisplay.formatPhone gains an optional country param; without it, legacy BR/international behavior is unchanged.
    - Signup payloads send whatsapp as E.164 with the country dial code (no '+'), defaulting to BR when the phone country is absent so existing tests stay green.
    - Signup pages show a DDI selector for the phone, defaulting to the active locale's country.
  </behavior>
  <action>
In `src/lib/reviewDisplay.ts`, change `formatPhone` to `formatPhone(value: string | undefined | null, country?: CountryCode): string`. When `country` is provided, return `phoneFormatPhone(value, country) || safeDisplayValue(value)` (import `formatPhone as phoneFormatPhone` from `./identifiers/phone` to avoid name clash) after the empty guard. When `country` is omitted, keep the current `formatWhatsapp(value)` behavior verbatim (access-request + invite depend on it). Import `type CountryCode` from `./identifiers/countryIdentifiers`.

In `src/features/individual-signup/individualSignupReview.ts`: add optional field `whatsappCountry?: CountryCode` to `IndividualSignupFormValues`. In `buildIndividualSignupPayload`, replace `whatsapp: toWhatsappApiValue(values.whatsapp)` with `whatsapp: toE164(values.whatsapp, values.whatsappCountry ?? 'BR')` (import `toE164` from `../../lib/identifiers`; you may drop the now-unused `toWhatsappApiValue` import if nothing else uses it). In the review sections, change the WhatsApp field to `value: formatPhone(values.whatsapp, values.whatsappCountry ?? 'BR')`.

Apply the identical changes to `src/features/company-signup/companySignupReview.ts` (`CompanySignupFormValues` gets `whatsappCountry?: CountryCode`; payload uses `toE164`; review uses `formatPhone(values.whatsapp, values.whatsappCountry ?? 'BR')`).

In `src/features/individual-signup/IndividualSignupPage.tsx`: add state `const [phoneCountry, setPhoneCountry] = useState<CountryCode>(() => defaultPhoneCountry(getActiveLocale()))` (import `defaultPhoneCountry` from `@/lib/identifiers`). Replace the `<WhatsappInput ... />` block with `<PhoneInput id="whatsapp" label="WhatsApp" value={whatsapp} onChange={setWhatsapp} country={phoneCountry} onCountryChange={setPhoneCountry} required />` (import PhoneInput from `@/components/ui/PhoneInput`; remove the WhatsappInput import). Add `whatsappCountry: phoneCountry` to the `formValues` useMemo object and add `phoneCountry` to its dependency array.

Apply the identical wiring to `src/features/company-signup/CompanySignupPage.tsx`.

Do NOT touch access-request or invite files. Do NOT remove `toWhatsappApiValue` from whatsapp.ts.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "signup|reviewDisplay|Signup" || echo "no signup/review type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `formatPhone('+55 54 99999-9999')` (no country) still returns '+55 54 99999-9999' (legacy path unchanged for access-request/invite).
    - `formatPhone('595981234567', 'PY')` returns '+595 981 234 567'; `formatPhone('12025550123', 'US')` returns '+1 (202) 555-0123'.
    - `buildIndividualSignupPayload({...whatsapp:'+55 54 99999-9999', whatsappCountry:'BR'}).whatsapp` === '5554999999999'.
    - `buildIndividualSignupPayload({...whatsapp:'+595 981 234 567', whatsappCountry:'PY'}).whatsapp` === '595981234567'.
    - `buildCompanySignupPayload({...whatsapp:'+1 (202) 555-0123', whatsappCountry:'US'}).whatsapp` === '12025550123'.
    - A form value omitting whatsappCountry still builds a BR payload (default 'BR') so tests/country-signup-integration.test.ts stays green.
    - Both signup pages render <PhoneInput> and no longer import WhatsappInput.
  </acceptance_criteria>
  <done>Both signup flows use country-aware phone entry, payload E.164 carries the dial code, review shows per-country formatting, and legacy callers/tests are unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests for BR/PY/US signup phone payload and review</name>
  <read_first>
    - tests/country-signup-integration.test.ts (base-form fixtures + node:test pattern to mirror)
    - src/features/individual-signup/individualSignupReview.ts (updated builders)
    - src/features/company-signup/companySignupReview.ts (updated builders)
  </read_first>
  <behavior>
    - Assert payload E.164 and review phone string for BR/PY/US across both signup flows.
  </behavior>
  <action>
Create `tests/phone-signup-integration.test.ts` (node:assert/strict + node:test). Build individual and company form fixtures for each country (BR/PY/US) with `whatsappCountry` set and a realistic `whatsapp` display string, then assert `buildIndividualSignupPayload(...).whatsapp` / `buildCompanySignupPayload(...).whatsapp` equal the expected E.164, and assert the WhatsApp review field value from `buildIndividualSignupReviewSections` / `buildCompanySignupReviewSections` equals the expected per-country formatted string. Include an explicit BR regression asserting the BR payload whatsapp is '5554999999999'.
  </action>
  <verify>
    <automated>npx tsx --test tests/phone-signup-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts individual+company BR payload whatsapp === '5554999999999'.
    - Test asserts PY payload whatsapp === '595981234567' and review field === '+595 981 234 567'.
    - Test asserts US payload whatsapp === '12025550123' and review field === '+1 (202) 555-0123'.
    - `npx tsx --test tests/phone-signup-integration.test.ts` exits 0.
  </acceptance_criteria>
  <done>Multi-country signup phone behavior is locked by passing integration tests with a BR regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> PhoneInput -> signup payload | untrusted phone value formatted client-side and sent to auth-service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | signup payload whatsapp | mitigate | value is normalized through phone.ts toE164 (digit-only, dial-code-bounded) before send; server-side auth-service validation is the authoritative gate (out of scope, deferred) |
| T-05-04 | Information Disclosure | review dialog phone display | accept | phone shown to the submitting user only (their own data), not masked in the review-before-submit screen by product design |
| T-05-SC | Tampering | package installs | accept | no npm/pip/cargo installs in this phase |
</threat_model>

<verification>
- `npx tsx --test tests/phone-signup-integration.test.ts` passes.
- `npx tsx --test tests/country-signup-integration.test.ts` still passes (BR default preserved).
- `npx tsc -p tsconfig.app.json --noEmit` adds no NEW errors from the modified files (feat/document-chat baseline errors excluded).
- Stage ONLY this plan's seven files (no `git add -A`).
</verification>

<success_criteria>
- Country/DDI selector present in both signup forms with locale default (TEL-02).
- Payload sends E.164 with dial code per country; BR byte-identical (TEL-01).
- Review shows per-country formatted phone; legacy formatPhone callers unaffected.
</success_criteria>

<output>
Create `.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-02-SUMMARY.md` when done.
</output>
