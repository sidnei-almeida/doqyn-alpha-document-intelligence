---
phase: 05-telefone-por-pais-integracao-no-cadastro
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/utils/contactNormalize.ts
  - tests/contact-recipient-phone.test.ts
autonomous: true
requirements: [TEL-03]

must_haves:
  truths:
    - "A phone already carrying an explicit +595 or +1 dial code normalizes to that country and is NOT prefixed with 55."
    - "A BR national 10-11 digit number without a dial code still gets 55 prepended (BR convenience preserved)."
    - "A bare number already starting with a known dial code (55/595) is not double-prefixed with 55."
    - "extractRecipientPhoneCountryCode returns 55, 595, or 1 for the matching E.164 shapes."
    - "maskRecipientPhoneForDisplay masks per country: +55 54 *****-9999, +595 98 *****-4567, +1 (202) *****-0123."
  artifacts:
    - path: "server/utils/contactNormalize.ts"
      provides: "Multi-country E.164 normalization/extraction/masking (55/595/1) without assuming +55"
      contains: "595"
    - path: "tests/contact-recipient-phone.test.ts"
      provides: "BR-green regression + PY/US + no-double-prefix coverage"
      contains: "595"
  key_links:
    - from: "tests/contact-recipient-phone.test.ts"
      to: "server/utils/contactNormalize.ts"
      via: "relative .js import"
      pattern: "contactNormalize\\.js"
---

<objective>
Generalize `server/utils/contactNormalize.ts` so it normalizes, extracts, and masks E.164 phone numbers for BR (+55), PY (+595) and US (+1) WITHOUT blindly assuming +55 — while keeping the existing BR flow and `tests/contact-recipient-phone.test.ts` green.

Purpose: Delivers TEL-03. The recipient-phone helpers (used by share/signature/invite/access-request) must stop forcing 55 onto numbers that already carry a known dial code, and must mask/extract country codes for the three supported countries. This is independent of the client work (no shared files) and runs in Wave 1.
Output: generalized `ensureBrCountryCode`, `extractRecipientPhoneCountryCode`, `maskRecipientPhoneForDisplay`; extended tests covering PY/US + no-double-prefix + BR regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-CONTEXT.md

<interfaces>
<!-- Current server helpers (to generalize) and their contract. -->

From server/utils/contactNormalize.ts:
- `parseOptionalRecipientPhone(value?): ParsedRecipientPhone | null` — respects a leading '+' (hasExplicitDdi); without '+' calls ensureBrCountryCode; validates via isValidWhatsapp (10-15 digits); returns `{ recipientPhone, recipientPhoneNormalized: '+'+digits, recipientPhoneCountryCode, recipientPhoneMasked }`.
- private `ensureBrCountryCode(digits)` — prepends '55' to bare 10-11 digit numbers not already starting with '55'.
- private `extractRecipientPhoneCountryCode(digits)` — currently BR-biased.
- `maskRecipientPhoneForDisplay(e164)` — currently BR-only detailed mask + generic fallback.
- `isValidWhatsapp(value)` — 10-15 digits; keep unchanged.
- `INVALID_RECIPIENT_PHONE_MESSAGE` — keep unchanged.

CRITICAL server convention: server/ files use RELATIVE imports with an explicit `.js` extension (never the `@/` alias). The existing test imports `from '../server/utils/contactNormalize.js'`. Match this exactly.

Ambiguity note (from CONTEXT): a bare (no '+') 11-digit number is genuinely ambiguous (BR São Paulo national DDD 11 vs US 1+10). The client is instructed to send US/PY WITH an explicit dial code, so disambiguation for non-BR comes from the '+'/known-dial-code path. Therefore: the no-'+' BR convenience (prepend 55 for 10-11 digits) is preserved as the default, and known-dial-code detection targets the unambiguous shapes (leading 55, or leading 595 with length 12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize normalization, country extraction, and masking for 55/595/1</name>
  <read_first>
    - server/utils/contactNormalize.ts (ensureBrCountryCode lines 18-26; extractRecipientPhoneCountryCode lines 51-58; maskRecipientPhoneForDisplay lines 36-49; parseOptionalRecipientPhone lines 64-93)
    - tests/contact-recipient-phone.test.ts (current BR assertions that must stay exactly green)
  </read_first>
  <behavior>
    - Explicit '+' dial code (55/595/1) is respected and never re-prefixed with 55.
    - No-'+' input: BR national 10-11 digits -> prepend 55; a number already starting with 595 (len 12) or 55 -> unchanged.
    - Country extraction recognizes 595 (len>=12 starting 595), 55 (len>=12 starting 55), 1 (len 11 starting 1).
    - Masking is country-specific for 55/595/1 with a safe generic fallback.
  </behavior>
  <action>
In `server/utils/contactNormalize.ts` add module constants for the known dial codes (e.g. `PY_COUNTRY_CODE = '595'`, `US_COUNTRY_CODE = '1'`, keep `BR_COUNTRY_CODE = '55'`).

Update `ensureBrCountryCode(digits)`: after digit-normalizing and the empty guard, return the digits UNCHANGED when they already start with a known dial code in an unambiguous shape — specifically `digits.startsWith('595')` with length >= 12, OR `digits.startsWith('55')`. Otherwise keep the existing BR convenience: if length is 10-11 prepend '55'; else return as-is. (Do NOT special-case a leading '1' here, to avoid breaking BR DDD-11 national numbers that legitimately start with '1'; US is disambiguated by the explicit '+' path in parseOptionalRecipientPhone.)

Update `extractRecipientPhoneCountryCode(digits)` to recognize all three, checked in this order: if `startsWith('595')` and length >= 12 -> '595'; else if `startsWith('55')` and length >= 12 -> '55'; else if `startsWith('1')` and length === 11 -> '1'; else fall back to the existing length-based logic.

Update `maskRecipientPhoneForDisplay(e164)` to mask per detected country using the same dial-code detection:
- BR (starts '55', len >= 12): keep existing `+55 <DDD> *****-<last4>` (DDD = digits[2..4], last4 = last 4).
- PY (starts '595', len === 12): national = digits.slice(3) (9 digits); area = national.slice(0,2); output `+595 <area2> *****-<last4>`.
- US (starts '1', len === 11): national = digits.slice(1) (10 digits); area = national.slice(0,3); output `+1 (<area3>) *****-<last4>`.
- else: keep the current generic fallback.

Leave `parseOptionalRecipientPhone` structure intact (it already respects the explicit '+' path and calls ensureBrCountryCode only when no '+'); it will now produce correct country/mask for PY/US inputs that carry an explicit '+'. Do NOT change `isValidWhatsapp` or `INVALID_RECIPIENT_PHONE_MESSAGE`.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.server.json --noEmit 2>&1 | grep -E "contactNormalize" || echo "no contactNormalize type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `parseOptionalRecipientPhone('+595 981 234 567').recipientPhoneNormalized` === '+595981234567', `.recipientPhoneCountryCode` === '595', `.recipientPhoneMasked` === '+595 98 *****-4567'.
    - `parseOptionalRecipientPhone('+1 (202) 555-0123').recipientPhoneNormalized` === '+12025550123', `.recipientPhoneCountryCode` === '1', `.recipientPhoneMasked` === '+1 (202) *****-0123'.
    - `parseOptionalRecipientPhone('(54) 99999-9999').recipientPhoneNormalized` === '+5554999999999', `.recipientPhoneCountryCode` === '55', `.recipientPhoneMasked` === '+55 54 *****-9999' (BR unchanged).
    - `parseOptionalRecipientPhone('+55 54 99999-9999').recipientPhoneNormalized` === '+5554999999999' (no double 55).
    - A bare '595981234567' passed without '+' is NOT prefixed with 55 (stays '595981234567' after ensureBrCountryCode).
    - `maskRecipientPhoneForDisplay('+5554999999999')` === '+55 54 *****-9999'.
  </acceptance_criteria>
  <done>contactNormalize normalizes/extracts/masks for 55/595/1 without forcing 55 onto known-dial-code numbers, BR convenience intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend contact-recipient-phone tests for PY/US and no-double-prefix</name>
  <read_first>
    - tests/contact-recipient-phone.test.ts (existing BR cases to keep verbatim; import style `../server/utils/contactNormalize.js`)
    - server/utils/contactNormalize.ts (updated helpers from Task 1)
  </read_first>
  <behavior>
    - Keep all existing BR assertions; add PY/US normalization+mask + no-double-prefix + BR-national-still-55 cases.
  </behavior>
  <action>
Extend `tests/contact-recipient-phone.test.ts` (do not remove existing `it` blocks). Add `it` cases asserting: PY explicit `+595 981 234 567` normalizes to '+595981234567' with country '595' and mask '+595 98 *****-4567'; US explicit `+1 (202) 555-0123' normalizes to '+12025550123' with country '1' and mask '+1 (202) *****-0123'; a number already starting with a known dial code is not re-prefixed with 55 (e.g. `+595...` stays 595-led); and a BR national 11-digit input without a dial code still becomes '55...' (reassert the existing '(54) 99999-9999' -> '+5554999999999'). Keep the relative `.js` import path.
  </action>
  <verify>
    <automated>npx tsx --test tests/contact-recipient-phone.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test tests/contact-recipient-phone.test.ts` exits 0 with all original BR cases still passing.
    - Test file contains explicit assertions on '+595981234567' and '+12025550123' and their masks.
    - Test file asserts a known-dial-code number is not double-prefixed with 55.
  </acceptance_criteria>
  <done>Server phone normalization is covered for BR (regression) + PY/US + no-double-prefix, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external invite/recipient input -> contactNormalize | untrusted phone strings from share/signature/invite/access-request cross into server normalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | parseOptionalRecipientPhone input | mitigate | RECIPIENT_PHONE_INPUT_PATTERN allowlist (`^[\d+\s()-]+$`) rejects other characters; digits capped at PHONE_MAX_DIGITS (15); isValidWhatsapp enforces 10-15 length |
| T-05-06 | Information Disclosure | phone in logs/UI | mitigate | maskRecipientPhoneForDisplay hides middle digits for 55/595/1 and generic fallback; only last 4 shown |
| T-05-SC | Tampering | package installs | accept | no npm/pip/cargo installs in this phase |
</threat_model>

<verification>
- `npx tsx --test tests/contact-recipient-phone.test.ts` passes (original BR cases + new PY/US + no-double-prefix).
- `npx tsc -p tsconfig.server.json --noEmit` adds no NEW errors from contactNormalize.ts (feat/document-chat baseline excluded).
- Stage ONLY server/utils/contactNormalize.ts and tests/contact-recipient-phone.test.ts (no `git add -A`).
</verification>

<success_criteria>
- contactNormalize accepts/normalizes multi-country E.164 (55/595/1) without assuming +55 (TEL-03).
- BR flow and existing tests unchanged/green.
- Country extraction + masking generalized for the three countries.
</success_criteria>

<output>
Create `.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-03-SUMMARY.md` when done.
</output>
