---
phase: 05-telefone-por-pais-integracao-no-cadastro
plan: 04
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/i18n/locales/pt-BR/auth.json
  - src/i18n/locales/es-PY/auth.json
  - src/i18n/locales/en-US/auth.json
  - src/i18n/index.ts
  - src/pages/Login.tsx
  - src/features/individual-signup/IndividualSignupPage.tsx
  - src/features/company-signup/CompanySignupPage.tsx
  - tests/i18n-auth-namespace.test.ts
autonomous: true
requirements: [TEL-02]

must_haves:
  truths:
    - "The auth namespace is registered in i18next for pt-BR, es-PY and en-US."
    - "Login.tsx renders its visible strings via t('auth:...')."
    - "Both signup pages render their visible strings (including the phone field label) via t('auth:...')."
    - "The auth catalogs have identical key sets across the three locales with no empty values."
  artifacts:
    - path: "src/i18n/locales/pt-BR/auth.json"
      provides: "pt-BR auth strings (login + individual/company signup)"
      contains: "login"
    - path: "src/i18n/locales/es-PY/auth.json"
      provides: "es-PY auth strings"
      contains: "login"
    - path: "src/i18n/locales/en-US/auth.json"
      provides: "en-US auth strings"
      contains: "login"
    - path: "tests/i18n-auth-namespace.test.ts"
      provides: "auth namespace key-parity + non-empty coverage"
      contains: "auth"
  key_links:
    - from: "src/i18n/index.ts"
      to: "src/i18n/locales/pt-BR/auth.json"
      via: "resources registration + ns array"
      pattern: "auth"
    - from: "src/pages/Login.tsx"
      to: "auth namespace"
      via: "useTranslation('auth') / t('auth:...')"
      pattern: "useTranslation\\('auth'\\)|auth:"
---

<objective>
Translate the visible strings of the Login page and both signup pages into an `auth` i18n namespace registered for pt-BR/es-PY/en-US, so the authentication/registration surface renders in the active language.

Purpose: Delivers ROADMAP Phase 5 success criterion 4 (cadastro/auth strings pass through `t()` in three locales) and localizes the phone field + DDI selector labels introduced by TEL-02. Depends on 05-02 because it edits the same two signup pages (must run after PhoneInput wiring lands).
Output: three `auth.json` catalogs; `auth` namespace registered in `src/i18n/index.ts`; Login + both signup pages migrated to `t('auth:...')`; a namespace parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-CONTEXT.md
@.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-02-SUMMARY.md

<interfaces>
<!-- i18n wiring + the pages to migrate. -->

From src/i18n/index.ts (current registration):
- `resources` maps each locale to `{ common, nav, identifiers }`; `ns: ['common', 'nav', 'identifiers']`; `defaultNS: 'common'`. Add `auth` to both.
- Catalogs are imported as JSON: e.g. `import ptBRIdentifiers from './locales/pt-BR/identifiers.json'`.

From react-i18next (already used across the app, e.g. CountrySelect.tsx):
- `const { t } = useTranslation('auth');` then `t('login.title')`, OR `const { t } = useTranslation();` then `t('auth:login.title')`.

Pages to migrate (visible pt-BR strings currently hardcoded):
- src/pages/Login.tsx: eyebrow 'Document Intelligence', title 'Entrar no sistema', description, footer link 'Não tem acesso ainda?', hints ('Use sua conta Google...', 'Acesse sua área...'), OAuth button labels ('Continuar com Google/Microsoft'), 'ou', field labels 'E-mail'/'Senha DOQYN' + placeholders, 'Lembrar acesso', 'Esqueci minha senha', submit label 'Entrando...'/'Entrar com e-mail e senha'.
- src/features/individual-signup/IndividualSignupPage.tsx: eyebrow 'Pessoa física', description, section 'Dados pessoais', labels Nome/Sobrenome/E-mail/WhatsApp/País/Senha/Confirmar senha, 'Voltar', 'Criar acesso CPF', validation/toast strings.
- src/features/company-signup/CompanySignupPage.tsx: eyebrow 'Cadastrar empresa', description, section 'Dados da empresa', labels Nome da empresa/País/Nome do responsável/Sobrenome/E-mail corporativo/WhatsApp/Senha/Confirmar senha, authorization text, 'Voltar', 'Cadastrar empresa', validation/toast strings.

From tests/i18n-catalogs.test.ts (the parity-test pattern to mirror in a NEW file): reads JSON catalogs with node:fs, asserts identical sorted key sets across locales and no empty values.

Convention: src/ imports use `@/` alias / extension-less relative paths; NO .js extension. `getActiveLocale`/labels already used in these pages stay.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth catalogs and register the auth namespace</name>
  <read_first>
    - src/i18n/index.ts (resources map, ns array, JSON import pattern)
    - src/i18n/locales/pt-BR/identifiers.json (small-catalog JSON shape analog)
    - src/pages/Login.tsx, src/features/individual-signup/IndividualSignupPage.tsx, src/features/company-signup/CompanySignupPage.tsx (source of the visible strings to key)
  </read_first>
  <action>
Create `src/i18n/locales/pt-BR/auth.json` with a nested key structure covering the three screens, e.g. groups `login.*`, `signup.common.*` (shared field labels: firstName, lastName, email, whatsapp, country, password, confirmPassword, back), `signup.individual.*`, `signup.company.*`. Use the current pt-BR hardcoded strings as the pt-BR values (verbatim, so pt-BR output is byte-identical to today). Keep interpolation where a string contains a dynamic value (e.g. terms version) using i18next `{{var}}` syntax.

Create `src/i18n/locales/es-PY/auth.json` and `src/i18n/locales/en-US/auth.json` with the SAME key structure and localized Spanish (Paraguay) and US English values respectively. Every key present in pt-BR must exist in all three, with no empty values.

In `src/i18n/index.ts`: add JSON imports `ptBRAuth`, `esPYAuth`, `enUSAuth` from the three new files; add `auth` to each locale's resources object; add `'auth'` to the `ns` array. Keep `defaultNS: 'common'` unchanged.
  </action>
  <verify>
    <automated>node -e "['pt-BR','es-PY','en-US'].forEach(l=>{const j=require('./src/i18n/locales/'+l+'/auth.json');const k=Object.keys(JSON.stringify(j)).length;if(!j.login)throw new Error('missing login group in '+l)});console.log('auth catalogs present')"</automated>
  </verify>
  <acceptance_criteria>
    - The three auth.json files exist and parse as JSON.
    - Each contains a `login` group and a `signup` group; key sets are identical across the three locales.
    - src/i18n/index.ts lists `'auth'` in `ns` and maps `auth` in resources for all three locales.
    - pt-BR values equal the current hardcoded pt-BR strings (no visible change for pt-BR users).
  </acceptance_criteria>
  <done>auth namespace is registered and populated for all three locales with identical keys.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Login and both signup pages to t('auth:...')</name>
  <read_first>
    - src/pages/Login.tsx (all hardcoded JSX strings)
    - src/features/individual-signup/IndividualSignupPage.tsx (hardcoded strings incl. phone/country field labels)
    - src/features/company-signup/CompanySignupPage.tsx (hardcoded strings incl. phone/country field labels)
    - src/components/ui/CountrySelect.tsx (useTranslation usage pattern to mirror)
    - src/i18n/locales/pt-BR/auth.json (keys created in Task 1)
  </read_first>
  <action>
In each of the three pages, add `const { t } = useTranslation('auth')` (import from `react-i18next`) and replace the visible hardcoded strings with `t('...')` calls against the keys created in Task 1. Cover the main visible surfaces: eyebrow/title/description, section headers, all field labels (including the WhatsApp/phone label and the País/country label passed to CountrySelect and PhoneInput), buttons, links (Voltar / footer), the OAuth button labels and hints on Login, and the submit/loading labels. For strings with a dynamic part (terms version, toast messages) use `t('key', { version: ... })` interpolation.

Do not change component logic, state, handlers, validation flow, or the PhoneInput/CountrySelect wiring from plan 05-02 — only swap literal display strings for `t()` calls. Secondary/rare strings (Claude's Discretion per CONTEXT) may remain if they are not primary visible copy, but all labels/buttons/titles/section headers must be translated.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "Login|Signup" || echo "no page type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Each page calls `useTranslation('auth')` and renders titles, section headers, field labels (incl. phone + country), and buttons via `t()`.
    - No primary visible label/button/title remains a hardcoded literal in the three pages.
    - PhoneInput/CountrySelect wiring and all handlers/validation are unchanged apart from label strings now coming from `t()`.
    - `npx tsc -p tsconfig.app.json --noEmit` adds no NEW errors from these pages.
  </acceptance_criteria>
  <done>Login + both signup pages render their visible copy from the auth namespace.</done>
</task>

<task type="auto">
  <name>Task 3: Test auth namespace key parity and non-empty values</name>
  <read_first>
    - tests/i18n-catalogs.test.ts (node:fs catalog-parity pattern to mirror)
    - src/i18n/locales/pt-BR/auth.json (structure to validate)
  </read_first>
  <action>
Create `tests/i18n-auth-namespace.test.ts` mirroring the i18n-catalogs.test.ts approach (node:assert/strict + node:test + node:fs reading the three `auth.json` files). Flatten nested keys (recurse into objects) and assert the flattened key sets are identical across pt-BR/es-PY/en-US, and that no leaf value is an empty string in any locale. Add an assertion that a representative pt-BR value (e.g. the login title 'Entrar no sistema') matches the current UI string to lock pt-BR back-compat. Do NOT modify tests/i18n-catalogs.test.ts.
  </action>
  <verify>
    <automated>npx tsx --test tests/i18n-auth-namespace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test tests/i18n-auth-namespace.test.ts` exits 0.
    - Test fails if any locale is missing a key present in another (parity enforced on flattened keys).
    - Test fails on any empty leaf value.
    - Asserts a known pt-BR value to guard against accidental pt-BR copy change.
  </acceptance_criteria>
  <done>auth catalog parity + non-empty + pt-BR regression are enforced by a passing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static JSON catalogs -> rendered UI | translation strings are developer-authored static assets, no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | auth.json catalogs | accept | static developer-authored assets; i18next `interpolation.escapeValue:false` is safe because React escapes rendered output by default (no dangerouslySetInnerHTML used on these strings) |
| T-05-08 | Information Disclosure | translated error copy | accept | error strings surface auth failure categories already shown today; no new secrets exposed |
| T-05-SC | Tampering | package installs | accept | no npm/pip/cargo installs in this phase (react-i18next already present) |
</threat_model>

<verification>
- `npx tsx --test tests/i18n-auth-namespace.test.ts` passes.
- `npx tsx --test tests/i18n-catalogs.test.ts` still passes (untouched).
- `npx tsc -p tsconfig.app.json --noEmit` adds no NEW errors from the three pages or i18n/index.ts (feat/document-chat baseline excluded).
- Stage ONLY this plan's eight files (no `git add -A`).
</verification>

<success_criteria>
- auth namespace registered and populated for pt-BR/es-PY/en-US with identical keys.
- Login + both signup pages render visible copy via t() (phone + country labels included → TEL-02 surface localized).
- pt-BR output unchanged; parity test green.
</success_criteria>

<output>
Create `.planning/phases/05-telefone-por-pais-integracao-no-cadastro/05-04-SUMMARY.md` when done.
</output>
