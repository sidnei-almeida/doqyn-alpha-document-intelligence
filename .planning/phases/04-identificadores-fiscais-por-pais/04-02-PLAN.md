---
phase: 04-identificadores-fiscais-por-pais
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/ui/DocumentIdInput.tsx
  - src/components/ui/CountrySelect.tsx
  - src/lib/reviewDisplay.ts
  - src/i18n/locales/pt-BR/identifiers.json
  - src/i18n/locales/es-PY/identifiers.json
  - src/i18n/locales/en-US/identifiers.json
  - src/i18n/index.ts
  - tests/country-review-display.test.ts
autonomous: true
requirements: [DOC-01, DOC-03, DOC-05]

must_haves:
  truths:
    - "DocumentIdInput renders the correct mask/placeholder/label for any (country, personType) and reports incompleteness using the spec"
    - "CountrySelect lists BR/PY/US with localized labels and is a controlled Select"
    - "formatDocumentForReview masks the personal ID per country (CPF 529.***.***-25, SSN ***-**-6789, CI *.***.567) and shows company IDs in full"
    - "Existing BR callers of formatDocument/formatDocumentForReview still compile and produce identical output"
    - "identifiers namespace is registered for all three locales"
  artifacts:
    - path: "src/components/ui/DocumentIdInput.tsx"
      provides: "Country-aware masked input built on useFormattedInput + getIdentifierSpec"
      exports: ["DocumentIdInput", "DocumentIdInputProps"]
      min_lines: 30
    - path: "src/components/ui/CountrySelect.tsx"
      provides: "Country picker over SUPPORTED_COUNTRIES with i18n labels"
      exports: ["CountrySelect"]
    - path: "src/lib/reviewDisplay.ts"
      provides: "formatDocument/formatDocumentForReview generalized to (value, country, personType) with BR back-compat"
      contains: "CountryCode"
    - path: "src/i18n/locales/pt-BR/identifiers.json"
      provides: "country + document label keys"
      contains: "country"
  key_links:
    - from: "src/components/ui/DocumentIdInput.tsx"
      to: "src/lib/identifiers/countryIdentifiers.ts"
      via: "getIdentifierSpec(country, personType)"
      pattern: "getIdentifierSpec"
    - from: "src/lib/reviewDisplay.ts"
      to: "src/lib/identifiers/countryIdentifiers.ts"
      via: "spec.format for per-country review formatting"
      pattern: "getIdentifierSpec|COUNTRY_IDENTIFIERS"
    - from: "src/i18n/index.ts"
      to: "src/i18n/locales/pt-BR/identifiers.json"
      via: "resources registration + ns array"
      pattern: "identifiers"
---

<objective>
Build the country-aware UI + review layer on top of the 04-01 registry: a `DocumentIdInput` (mask/placeholder/validation from `getIdentifierSpec`), a `CountrySelect` over `SUPPORTED_COUNTRIES`, a generalized `reviewDisplay` that formats/masks documents per country, and the `identifiers` i18n catalogs (country + document names) for pt-BR/es-PY/en-US.

Purpose: These are the reusable pieces the signup pages (04-03) drop in. The `TaxIdInput` and existing BR review callers must keep working untouched.

Output: `DocumentIdInput.tsx`, `CountrySelect.tsx`, generalized `reviewDisplay.ts` (BR-compatible), three `identifiers.json` catalogs, updated `i18n/index.ts`, and a review-display test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-identificadores-fiscais-por-pais/04-CONTEXT.md
@.planning/phases/04-identificadores-fiscais-por-pais/04-01-SUMMARY.md

<interfaces>
<!-- Registry from 04-01 — consume directly, do not re-explore. -->
From src/lib/identifiers/countryIdentifiers.ts:
  type CountryCode = 'BR' | 'PY' | 'US';
  type PersonType = 'individual' | 'company';
  interface IdentifierSpec { code: string; labelKey: string; format(raw): string; placeholder: string; normalize(raw): string; isComplete(raw): boolean; validate(raw): boolean; inputMode: 'numeric' | 'text'; }
  const COUNTRY_IDENTIFIERS: Record<CountryCode, Record<PersonType, IdentifierSpec>>;
  function getIdentifierSpec(country, personType): IdentifierSpec;
  const SUPPORTED_COUNTRIES: CountryCode[];
  function countryLabelKey(country): string;
  function defaultCountryForLocale(locale): CountryCode;

From src/hooks/useFormattedInput.ts:
  useFormattedInput({ value, onChange, format }) -> { value, onChange, onPaste };  // controlled, masks on type/paste

From src/components/ui/Input.tsx:
  Input, InputProps  // supports value/onChange/onPaste/error/placeholder/inputMode/label

From src/components/ui/Select.tsx:
  Select (props: label, error, options: {value,label}[], value, onChange: (e: ChangeEvent<HTMLSelectElement>) => void)

From src/lib/reviewDisplay.ts (current BR-only shape to generalize — keep back-compat):
  formatDocument(value: string, kind: TaxIdKind = 'CNPJ'): string
  formatDocumentForReview(value: string, kind: TaxIdKind): string
  // callers pass 'CPF' | 'CNPJ' today (individualSignupReview, companySignupReview, requestAccessReview)

From src/components/ui/TaxIdInput.tsx (reference pattern — leave file unchanged):
  useFormattedInput + formatTaxId(kind) + isCompleteTaxId(kind) + taxIdPlaceholder(kind), error `${kind} incompleto.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize reviewDisplay to (value, country, personType) with BR back-compat</name>
  <files>src/lib/reviewDisplay.ts, tests/country-review-display.test.ts</files>
  <read_first>
    - src/lib/reviewDisplay.ts (current formatDocument/formatDocumentForReview + CPF masking to preserve)
    - src/lib/identifiers/countryIdentifiers.ts (getIdentifierSpec, CountryCode, PersonType)
    - src/features/individual-signup/individualSignupReview.ts (BR caller passing 'CPF')
    - src/features/company-signup/companySignupReview.ts (BR caller passing 'CNPJ')
    - src/features/access-request/requestAccessReview.ts (BR caller passing dynamic 'CPF'|'CNPJ')
  </read_first>
  <behavior>
    - formatDocumentForReview('52998224725', 'BR', 'individual') === '529.***.***-25'  (unchanged CPF mask shape)
    - formatDocumentForReview('11222333000181', 'BR', 'company') === '11.222.333/0001-81'  (CNPJ full)
    - formatDocumentForReview('123456789', 'US', 'individual') === '***-**-6789'  (SSN mask last 4)
    - formatDocumentForReview('123456789', 'US', 'company') === '12-3456789'  (EIN full)
    - formatDocumentForReview('1234567', 'PY', 'individual') === '*.***.567'  (CI reveal last 3)
    - formatDocumentForReview('800177266', 'PY', 'company') === '80017726-6'  (RUC full)
    - formatDocument('12345678901', 'BR', 'individual') === '123.456.789-01'  (full, unmasked)
    - Legacy call formatDocumentForReview('52998224725', 'CPF') still returns '529.***.***-25' (back-compat overload)
    - Legacy call formatDocument('11222333000181', 'CNPJ') still returns '11.222.333/0001-81'
    - Empty value returns '—'
  </behavior>
  <action>
    Generalize `src/lib/reviewDisplay.ts` so `formatDocument` and `formatDocumentForReview` accept `(value: string, country: CountryCode, personType: PersonType)`. Implement via TypeScript function overloads so the OLD BR signatures keep compiling: accept a second arg that is either a `TaxIdKind` ('CPF'|'CNPJ') OR a `CountryCode`. When the second arg is 'CPF'/'CNPJ', internally map to country 'BR' and personType 'individual'(CPF)/'company'(CNPJ) and a third arg is not required. When it is a CountryCode, require the third `personType` arg. Do not break the three existing BR callers.

    `formatDocument(value, country, personType)`: normalize via the spec; if empty digits return '—'; else return `getIdentifierSpec(country, personType).format(digits)` (full, unmasked).

    `formatDocumentForReview(value, country, personType)`: normalize; empty -> '—'. For `personType === 'company'` return the full `spec.format(digits)` (no masking — matches current CNPJ behavior). For `personType === 'individual'` apply per-country masking on the normalized digits:
      - BR (CPF): preserve the EXACT current shape `${d.slice(0,3)}.***.***-${d.slice(-2)}` when 11 digits; when fewer than 11 digits fall back to `spec.format(d)` (matches today's partial behavior).
      - US (SSN): `***-**-${d.slice(-4)}` when 9 digits; fewer -> `spec.format(d)`.
      - PY (CI): reveal only the last 3 digits, mask the rest with `*`, then apply the same dotted-thousand grouping as the CI format — e.g. `1234567` -> `*.***.567`; when fewer than 4 digits return `spec.format(d)`.

    Keep `safeDisplayValue`, `formatPhone`, `formatBooleanConsent`, `PASSWORD_REVIEW_LABEL` exactly as-is. Update imports to pull `getIdentifierSpec`/types from `./identifiers/countryIdentifiers` (keep the existing `formatCnpj`/`normalizeTaxId` imports only if still used by the back-compat path).

    Create `tests/country-review-display.test.ts` asserting every behavior case above, INCLUDING the two legacy-overload calls to prove BR back-compat.
  </action>
  <verify>
    <automated>npx tsx --test tests/country-review-display.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both functions accept `(value, country, personType)` AND the legacy `(value, 'CPF'|'CNPJ')` form via overloads; all three existing BR review modules compile without edits (verified by `npx tsc -p tsconfig.app.json --noEmit` showing no new errors in those files).
    - Masking outputs match exactly: CPF `529.***.***-25`, SSN `***-**-6789`, CI `*.***.567`; company docs (CNPJ/EIN/RUC) render in full.
    - Empty input returns '—'.
    - `npx tsx --test tests/country-review-display.test.ts` passes.
  </acceptance_criteria>
  <done>reviewDisplay generalized with BR-compatible overloads and per-country personal-ID masking; test green; existing BR callers untouched and still compile.</done>
</task>

<task type="auto">
  <name>Task 2: DocumentIdInput + CountrySelect components + identifiers i18n catalogs</name>
  <files>src/components/ui/DocumentIdInput.tsx, src/components/ui/CountrySelect.tsx, src/i18n/locales/pt-BR/identifiers.json, src/i18n/locales/es-PY/identifiers.json, src/i18n/locales/en-US/identifiers.json, src/i18n/index.ts</files>
  <read_first>
    - src/components/ui/TaxIdInput.tsx (pattern to mirror; leave this file unchanged)
    - src/hooks/useFormattedInput.ts (format/onChange/onPaste contract)
    - src/components/ui/Input.tsx (InputProps — error/placeholder/inputMode/label)
    - src/components/ui/Select.tsx (Select props + ChangeEvent<HTMLSelectElement> onChange shape)
    - src/lib/identifiers/countryIdentifiers.ts (getIdentifierSpec, SUPPORTED_COUNTRIES, countryLabelKey, CountryCode, PersonType)
    - src/i18n/index.ts (resources map + ns array to extend)
    - src/i18n/locales/pt-BR/common.json (JSON catalog shape/nesting)
  </read_first>
  <action>
    Create `src/components/ui/DocumentIdInput.tsx` exporting `DocumentIdInput` and `DocumentIdInputProps`. Props extend `Omit<InputProps, 'value'|'onChange'|'type'>` and add `country: CountryCode`, `personType: PersonType`, `value: string`, `onChange: (value: string) => void`. Inside: `const spec = getIdentifierSpec(country, personType)`; build a memoized `format = (raw) => spec.format(raw)` keyed on `[spec]`; feed `useFormattedInput({ value, onChange, format })`; render `Input` with the spread input props, `inputMode={spec.inputMode}`, `autoComplete="off"`, `placeholder={props.placeholder ?? spec.placeholder}`. Compute incompleteness `value.length > 0 && !spec.isComplete(value)` and, using `useTranslation('identifiers')`, surface an error like `t('incomplete', { doc: t(spec.labelKey) })` (fall back to `error ?? validationError`). Mirror `TaxIdInput`'s structure; do NOT modify `TaxIdInput.tsx`.

    Create `src/components/ui/CountrySelect.tsx` exporting `CountrySelect`. Props: `{ value: CountryCode; onChange: (country: CountryCode) => void; label?: string; error?: string; id?: string }`. Use `useTranslation('identifiers')`; build `options` from `SUPPORTED_COUNTRIES` mapping each to `{ value: code, label: t(countryLabelKey(code)) }`; render `Select` (value=value, onChange forwards `e.target.value as CountryCode`). Keep it presentation-only — no locale/state side effects; the parent owns the selected country.

    Create the three `identifiers.json` catalogs. Nesting: `country` object with `BR`/`PY`/`US` display names; `doc` object with `cpf`,`cnpj`,`ci`,`ruc`,`ssn`,`ein`; and an `incomplete` string with a `{{doc}}` interpolation. Values:
      - pt-BR: country BR="Brasil", PY="Paraguai", US="Estados Unidos"; doc names as the acronyms (CPF, CNPJ, CI, RUC, SSN, EIN); incomplete="{{doc}} incompleto.".
      - es-PY: country BR="Brasil", PY="Paraguay", US="Estados Unidos"; same acronyms; incomplete="{{doc}} incompleto.".
      - en-US: country BR="Brazil", PY="Paraguay", US="United States"; same acronyms; incomplete="{{doc}} is incomplete.".

    Register the namespace in `src/i18n/index.ts`: import the three `identifiers.json` files, add `identifiers: <catalog>` under each locale in the `resources` object, and add `'identifiers'` to the `ns` array. Do not change `defaultNS` or any other init option.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "DocumentIdInput|CountrySelect|identifiers|reviewDisplay" || echo "no new type errors in phase-04 files"</automated>
  </verify>
  <acceptance_criteria>
    - `DocumentIdInput.tsx` exports `DocumentIdInput`+`DocumentIdInputProps`, reads mask/placeholder/completeness/inputMode from `getIdentifierSpec(country, personType)`, and builds on `useFormattedInput`; `TaxIdInput.tsx` is unchanged.
    - `CountrySelect.tsx` renders a `Select` with one option per `SUPPORTED_COUNTRIES` labelled via `countryLabelKey`, controlled by `value`/`onChange`.
    - All three `identifiers.json` files exist with matching key structure (`country.{BR,PY,US}`, `doc.{cpf,cnpj,ci,ruc,ssn,ein}`, `incomplete`).
    - `src/i18n/index.ts` registers `identifiers` in `resources` for all three locales and includes `'identifiers'` in `ns`.
    - `npx tsc -p tsconfig.app.json --noEmit` shows no NEW errors in the phase-04 files (pre-existing feat/document-chat errors excluded).
  </acceptance_criteria>
  <done>DocumentIdInput + CountrySelect created on the registry, identifiers catalogs added for all three locales, namespace registered, no new type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → masked input component | Raw keystrokes/paste normalized+formatted client-side before onChange emits |
| stored ID → review render | Personal IDs partially masked before display in the review dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | formatDocumentForReview | mitigate | Personal IDs (CPF/SSN/CI) masked in review (reveal only leading/trailing few digits); company IDs shown in full by design. |
| T-04-04 | Tampering | i18n label interpolation | accept | `escapeValue:false` already set app-wide; label values are static catalog strings, not user input — no injection surface. |
</threat_model>

<verification>
- `npx tsx --test tests/country-review-display.test.ts` green (per-country masking + BR back-compat overloads).
- `npx tsc -p tsconfig.app.json --noEmit` reports no new errors in DocumentIdInput/CountrySelect/reviewDisplay/i18n.
- All three `identifiers.json` share the same key shape.
</verification>

<success_criteria>
Country-aware input, country picker, generalized+masking reviewDisplay (BR back-compatible), and the identifiers i18n catalogs exist and type-check; existing BR review callers keep compiling and producing identical output.
</success_criteria>

<output>
Create `.planning/phases/04-identificadores-fiscais-por-pais/04-02-SUMMARY.md` when done.
Stage ONLY this plan's files. Do NOT `git add -A` (working tree carries pre-existing feat/document-chat changes/failures).
</output>
