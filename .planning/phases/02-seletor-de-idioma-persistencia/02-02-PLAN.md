---
phase: 02-seletor-de-idioma-persistencia
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/ui/LanguageSelect.tsx
  - src/components/layout/HeaderUserMenu.tsx
  - src/features/settings/components/sections/PreferencesSettingsSection.tsx
  - tests/i18n-language-selector.test.ts
autonomous: true
requirements: [SEL-01, SEL-03]

must_haves:
  truths:
    - "User can change the language from a control in the header user menu"
    - "User can change the language from a control in Settings > Preferences"
    - "Both controls show the three native language names and highlight the active locale"
    - "Selecting a language switches the UI immediately without a page reload"
  artifacts:
    - path: "src/components/ui/LanguageSelect.tsx"
      provides: "Reusable language selector driven by useLocale + language.* keys"
      contains: "useLocale"
    - path: "src/components/layout/HeaderUserMenu.tsx"
      provides: "Language control in the account popover near ThemeToggle"
      contains: "LanguageSelect"
    - path: "src/features/settings/components/sections/PreferencesSettingsSection.tsx"
      provides: "Language SettingsRow in Preferences"
      contains: "LanguageSelect"
    - path: "tests/i18n-language-selector.test.ts"
      provides: "source-assertion coverage for both placements + hook wiring"
  key_links:
    - from: "src/components/ui/LanguageSelect.tsx"
      to: "src/i18n/useLocale.ts"
      via: "useLocale() drives options + setLocale on change"
      pattern: "useLocale"
    - from: "src/components/layout/HeaderUserMenu.tsx"
      to: "src/components/ui/LanguageSelect.tsx"
      via: "renders LanguageSelect in the popover"
      pattern: "LanguageSelect"
    - from: "src/features/settings/components/sections/PreferencesSettingsSection.tsx"
      to: "src/components/ui/LanguageSelect.tsx"
      via: "renders LanguageSelect inside a SettingsRow"
      pattern: "LanguageSelect"
---

<objective>
Build one reusable `LanguageSelect` component (driven by `useLocale` + the `language.*` catalog keys from plan 01) and mount it in BOTH the header account popover (`HeaderUserMenu`, next to `ThemeToggle`) and Settings > Preferences (`PreferencesSettingsSection`, reusing its `SettingsRow`). This is the visible half of SEL-01 and completes SEL-03 end to end.

Purpose: Gives users the actual switch in the two required surfaces, reusing the existing design system (D-05).
Output: `src/components/ui/LanguageSelect.tsx` plus wiring edits to `HeaderUserMenu.tsx` and `PreferencesSettingsSection.tsx`, and a source-assertion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-seletor-de-idioma-persistencia/02-CONTEXT.md
@.planning/phases/02-seletor-de-idioma-persistencia/02-01-PLAN.md

<interfaces>
<!-- Contracts from plan 01 (wave 1) this plan consumes directly. -->

From src/i18n/useLocale.ts (plan 02-01):
```typescript
export function useLocale(): {
  locale: SupportedLocale;
  setLocale: (locale: SupportedLocale) => void; // changeLanguage + persist, live, no reload
  supportedLocales: readonly SupportedLocale[]; // ['pt-BR','es-PY','en-US']
};
```

Catalog keys (common namespace, plan 02-01): `language.label`, `language.pt-BR` ("Português"), `language.es-PY` ("Español"), `language.en-US` ("English").

From src/features/settings/components/sections/PreferencesSettingsSection.tsx (segmented-control pattern to mirror):
```tsx
<SettingsRow label="Tema" description="..." control={
  <div className="settings-segmented-control" role="radiogroup" aria-label="...">
    {OPTIONS.map((o) => (
      <button role="radio" aria-checked={active} onClick={() => set(o.value)}
        className={cn('settings-segmented-control__item', active && 'settings-segmented-control__item--active')}>
        <Icon name={o.icon} size={ICON_SIZE.xs} aria-hidden />{o.label}
      </button>
    ))}
  </div>
} />
```

From src/components/layout/HeaderUserMenu.tsx (Tema row analog inside the AnchoredPopover, to place the language control near):
```tsx
<div className="flex items-center justify-between gap-2 px-3 py-2">
  <span className="text-[13px] text-doqyn-text">Tema</span>
  <ThemeToggle />
</div>
```

react-i18next `useTranslation('common')` provides `t`; components already use it across the shell (Phase 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable LanguageSelect component</name>
  <files>src/components/ui/LanguageSelect.tsx</files>
  <read_first>
    - src/i18n/useLocale.ts (plan 02-01 hook contract: locale, setLocale, supportedLocales)
    - src/components/ui/ThemeToggle.tsx (design-system button/aria/cn conventions analog)
    - src/features/settings/components/sections/PreferencesSettingsSection.tsx (settings-segmented-control markup + aria-checked radiogroup pattern to mirror)
    - src/i18n/config.ts (SupportedLocale, SUPPORTED_LOCALES)
  </read_first>
  <action>
    Create `src/components/ui/LanguageSelect.tsx` exporting a `LanguageSelect` component that renders a compact segmented control (radiogroup) with one option per `supportedLocales` entry from `useLocale()`. For each locale, render a `role="radio"` button with `aria-checked` bound to whether it equals the current `locale`, calling `setLocale(loc)` on click; the visible label comes from `t(\`language.${loc}\`, { ns: 'common' })` (native name). Wrap the group in `role="radiogroup"` with `aria-label={t('language.label', { ns: 'common' })}`. Reuse the existing `settings-segmented-control` / `settings-segmented-control__item(--active)` classes and the `cn` helper so it visually matches the Theme/View controls; accept an optional `className` prop merged onto the root via `cn` so the header can constrain width. Use `useTranslation('common')` for `t`. Import `SupportedLocale` with `import type`; alias imports via `@/*`. Do not read localStorage or call changeLanguage directly here — all state flows through `useLocale`. Keep it presentation-only and reusable by both header and settings (D-05); do not hardcode any language display string (all come from `language.*` keys).
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep "LanguageSelect.tsx" || echo "no LanguageSelect type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useLocale" src/components/ui/LanguageSelect.tsx` >= 1.
    - `grep -c "language.label\|language.\${\|language\." src/components/ui/LanguageSelect.tsx` >= 1 (labels sourced from catalog keys, not hardcoded).
    - No hardcoded native names: `grep -Ec "Português|Español|English" src/components/ui/LanguageSelect.tsx` == 0.
    - `grep -c "role=\"radiogroup\"" src/components/ui/LanguageSelect.tsx` >= 1 and `aria-checked` present.
    - `npx tsc -p tsconfig.app.json --noEmit` reports no errors in `src/components/ui/LanguageSelect.tsx`.
    - `npx eslint src/components/ui/LanguageSelect.tsx` returns zero issues.
  </acceptance_criteria>
  <done>A reusable, catalog-driven language selector exists that switches locale via useLocale and highlights the active language.</done>
</task>

<task type="auto">
  <name>Task 2: Mount LanguageSelect in header popover and Settings preferences</name>
  <files>src/components/layout/HeaderUserMenu.tsx, src/features/settings/components/sections/PreferencesSettingsSection.tsx</files>
  <read_first>
    - src/components/layout/HeaderUserMenu.tsx (the "Tema" row inside AnchoredPopover where the language control goes near ThemeToggle)
    - src/features/settings/components/sections/PreferencesSettingsSection.tsx (SettingsRow/SettingsRowList composition; where to insert a new language row)
    - src/components/ui/LanguageSelect.tsx (from Task 1)
  </read_first>
  <action>
    In `HeaderUserMenu.tsx`, import `LanguageSelect` from `@/components/ui/LanguageSelect` and add a language block inside the `AnchoredPopover`, adjacent to the existing Tema/`ThemeToggle` row (D-05: header, near ThemeToggle). Follow the same row shell as the Tema row: a label span using `t('language.label', { ns: 'common' })` and the `LanguageSelect` control; because the popover is narrow (w-56), give `LanguageSelect` a `className` that lets the segmented control wrap or shrink (e.g. full-width row above the control rather than inline) — choose the layout that keeps all three native names legible. Add `useTranslation('common')` if the component does not already have `t` in scope. Do not remove or alter the existing Settings link, Tema row, or Sair button.
    In `PreferencesSettingsSection.tsx`, import `LanguageSelect` and add a new `SettingsRow` inside the existing `SettingsRowList` (place it logically near the Tema row) with `label={t('language.label', ...)}` (add `useTranslation('common')` — this file currently uses hardcoded pt-BR labels, so introduce `t` for the new row's label/description without rewriting the existing Tema/Visualização rows) and a short description, passing `<LanguageSelect />` as the `control`. Reuse `SettingsRow` exactly like the Tema/Visualização rows; do not touch the existing theme or default-view logic.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "HeaderUserMenu|PreferencesSettingsSection" || echo "no wiring type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LanguageSelect" src/components/layout/HeaderUserMenu.tsx` >= 2 (import + usage).
    - `grep -c "LanguageSelect" src/features/settings/components/sections/PreferencesSettingsSection.tsx` >= 2 (import + usage).
    - Both files reference `language.label` via `t(...)` for the control label: `grep -c "language.label" <each file>` >= 1.
    - Existing header items intact: `grep -c "ThemeToggle" src/components/layout/HeaderUserMenu.tsx` >= 1 and the Sair/logout button remains.
    - Existing preferences logic intact: `grep -c "getLibraryDefaultView\|setTheme" src/features/settings/components/sections/PreferencesSettingsSection.tsx` >= 1.
    - `npx tsc -p tsconfig.app.json --noEmit` and `npx eslint` on both files return no new errors.
  </acceptance_criteria>
  <done>The language selector is reachable from both the header account popover and Settings > Preferences, reusing existing design-system primitives.</done>
</task>

<task type="auto">
  <name>Task 3: Source-assertion test for selector wiring in both surfaces</name>
  <files>tests/i18n-language-selector.test.ts</files>
  <read_first>
    - tests/i18n-shell-nav.test.ts (source-assertion test style: readFileSync from src root, node:test/assert)
    - src/components/ui/LanguageSelect.tsx (Task 1)
    - src/components/layout/HeaderUserMenu.tsx (Task 2)
    - src/features/settings/components/sections/PreferencesSettingsSection.tsx (Task 2)
  </read_first>
  <action>
    Create `tests/i18n-language-selector.test.ts` mirroring the readFileSync-from-`src` source-assertion approach of `tests/i18n-shell-nav.test.ts` (node:test `describe`/`it`, `node:assert/strict`). Assert: (1) `src/components/ui/LanguageSelect.tsx` source includes `useLocale` and references `language.` catalog keys and includes `role="radiogroup"`; (2) it does NOT hardcode native names (source excludes `'Português'`, `'Español'`, `'English'`); (3) `src/components/layout/HeaderUserMenu.tsx` source includes `LanguageSelect`; (4) `src/features/settings/components/sections/PreferencesSettingsSection.tsx` source includes `LanguageSelect`. Also assert all three `common.json` catalogs contain a `language` object with the native names (parse via JSON.parse of each file) to guard plan-01 keys the UI depends on. Keep the test DOM-free (pure source/JSON assertions, no React render).
  </action>
  <verify>
    <automated>npm test -- tests/i18n-language-selector.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/i18n-language-selector.test.ts` passes under `npm test`.
    - Test asserts LanguageSelect presence in BOTH `HeaderUserMenu.tsx` and `PreferencesSettingsSection.tsx` (SEL-01 both surfaces).
    - Test asserts LanguageSelect wires `useLocale` and sources labels from `language.*` keys (no hardcoded native names).
    - Test parses all three `common.json` and asserts the `language` native-name keys exist.
  </acceptance_criteria>
  <done>Regression coverage locks in both required placements and the catalog-driven, hook-driven wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI selection → i18n runtime | User picks a locale from a fixed set; value flows through useLocale.setLocale (already validated to SUPPORTED_LOCALES by plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | LanguageSelect option values | mitigate | Options are generated only from `supportedLocales` returned by useLocale (the SUPPORTED_LOCALES allowlist); no free-text input reaches setLocale |
| T-02-04 | Information Disclosure | rendered language labels | accept | Labels are static UI copy from bundled catalogs; no PII, no sensitive data |
</threat_model>

<verification>
- `npm test -- tests/i18n-language-selector.test.ts` passes.
- `npx tsc -p tsconfig.app.json --noEmit` shows no new errors in the three touched source files (pre-existing baseline errors in unrelated files are out of scope).
- `npx eslint src/components/ui/LanguageSelect.tsx src/components/layout/HeaderUserMenu.tsx src/features/settings/components/sections/PreferencesSettingsSection.tsx` returns zero issues.
</verification>

<success_criteria>
- Language selector present and functional in BOTH header popover and Settings > Preferences (SEL-01).
- Selecting a language switches UI immediately with no reload, persisted across sessions (SEL-03, closing the loop with plan 01).
- Selector reuses existing design-system primitives; labels come entirely from `language.*` catalog keys.
- New/changed files lint- and type-clean; new test green.
</success_criteria>

<output>
Create `.planning/phases/02-seletor-de-idioma-persistencia/02-02-SUMMARY.md` when done.
</output>
