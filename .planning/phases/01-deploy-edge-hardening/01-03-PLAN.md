---
phase: 01-deploy-edge-hardening
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - deploy/scripts/edge-smoke-check.sh
autonomous: false
requirements: [INFRA-01, AUTHSEC-03, SEC-01]
must_haves:
  truths:
    - "A reusable smoke-check script exists that verifies the five security headers and the rate-limit 429 behavior against a running host"
    - "curl -I against a production route returns all five security headers"
    - "A rapid burst against a public route returns 429/503 once the per-IP limit is exceeded, while a normal request to the same route returns its usual status"
    - "The guest share page and guest sign page render (PDF preview + signature image) with no CSP violations in the browser console"
    - "docker compose config shows resource limits on every service"
  artifacts:
    - path: "deploy/scripts/edge-smoke-check.sh"
      provides: "Repeatable header + rate-limit verification against a target host"
      contains: "Content-Security-Policy"
  key_links:
    - from: "deploy/scripts/edge-smoke-check.sh"
      to: "running nginx edge (target host arg)"
      via: "curl -I header assertions + burst loop asserting 429"
      pattern: "curl -I"
---

<objective>
Prove the wave-1 edge-hardening changes actually work at runtime and do not break legitimate guest flows. Produce a reusable smoke-check script (automating the header + rate-limit assertions) and a blocking human-verify checkpoint where the operator runs it against their deploy and confirms the guest share/sign pages still render under the new CSP.

Purpose: The config-level checks in plans 01-01/01-02 (`docker compose config`, `nginx -t`) prove syntax and structure, but the acceptance criteria for this phase are behavioral: real headers on `curl -I`, real 429s under burst, and — the explicitly-flagged new risk — a CSP that does NOT break the guest PDF-preview / signature-image rendering. Those require a running stack and a human eye on the browser console.

Output: `deploy/scripts/edge-smoke-check.sh` (reusable), plus operator sign-off that headers, rate limiting, resource limits, and guest rendering all behave correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@deploy/nginx/default.conf
@deploy/docker-compose.production.yml
@.planning/phases/01-deploy-edge-hardening/01-01-SUMMARY.md
@.planning/phases/01-deploy-edge-hardening/01-02-SUMMARY.md

<constraints>
- The stack requires real secrets (Postgres password, R2 creds, Groq key) and builds the sibling auth-service — bringing it up is the operator's action against their VPS or a local full-env, NOT something to fake. The script must accept a target base URL (default `http://localhost` / `${EDGE_BASE_URL}`) so it runs against whatever the operator has running.
- HSTS may only be observable over HTTPS depending on where TLS terminates; the script must assert the header is PRESENT in the response nginx emits (it is added with `always`), and note in output if the target is plain http.
- Do NOT weaken the CSP to make the checkpoint pass. If the guest pages break, the fix is to correct the CSP allowances in plan 01-02, not to relax the checkpoint.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the reusable edge smoke-check script</name>
  <files>deploy/scripts/edge-smoke-check.sh</files>
  <read_first>
    - deploy/nginx/default.conf (final state — the exact header names and the four rate-limited route prefixes)
    - deploy/scripts/ (list existing scripts, e.g. validate-vps-ready.sh, to match shell style/shebang/error-handling conventions)
  </read_first>
  <action>
    Create `deploy/scripts/edge-smoke-check.sh` (bash, `set -euo pipefail`, executable). It takes a base URL from `${EDGE_BASE_URL:-http://localhost}` (or first CLI arg). It must: (1) run `curl -sI "$BASE/"` and assert each of the five headers is present — Content-Security-Policy, Strict-Transport-Security, X-Frame-Options, X-Content-Type-Options, Referrer-Policy — failing with a clear message naming any missing header; (2) fire a burst (e.g. 60 sequential `curl -s -o /dev/null -w "%{http_code}"`) at `$BASE/api/share/smoke-test-token` and assert at least one `429` appears in the collected status codes, then assert a single spaced-out request to the same prefix does NOT return 429 (limit resets); (3) print a PASS/FAIL summary line per check and exit non-zero if any assertion fails. Print a note if `$BASE` is plain http (HSTS only enforced over https).
    Also have the script echo the reminder that the guest-page CSP render check is manual (covered by Task 2).
  </action>
  <verify>
    <automated>cd /home/a1rm4x/Documents/GitHub/doqyn-alpha-document-intelligence && bash -n deploy/scripts/edge-smoke-check.sh && test -x deploy/scripts/edge-smoke-check.sh && grep -q "Content-Security-Policy" deploy/scripts/edge-smoke-check.sh && grep -q "429" deploy/scripts/edge-smoke-check.sh && echo SCRIPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy/scripts/edge-smoke-check.sh` passes (valid syntax) and the file is executable.
    - The script asserts all five header names and asserts a `429` appears under burst.
    - The script parameterizes the target via `EDGE_BASE_URL` / CLI arg and exits non-zero on any failed assertion.
  </acceptance_criteria>
  <done>A reusable, parameterized smoke-check script exists that automates the header and rate-limit assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Docker resource limits on every service (plan 01-01), the five nginx security headers with a guest-scoped CSP, and per-IP rate limiting on the four public routes (plan 01-02), plus a reusable `deploy/scripts/edge-smoke-check.sh`.
  </what-built>
  <how-to-verify>
    1. Bring up the production stack against your target (VPS or full local env): `docker compose -f deploy/docker-compose.production.yml --env-file deploy/.env up -d` (add `--profile local-mongo` if self-hosting Mongo).
    2. Resource limits: run `docker compose -f deploy/docker-compose.production.yml --env-file deploy/.env config | grep -A4 resources` and confirm every service shows memory + cpu limits; optionally `docker stats` to confirm ceilings are applied.
    3. Headers + rate limiting: run `EDGE_BASE_URL=https://<your-host> deploy/scripts/edge-smoke-check.sh` and confirm it prints PASS for all five headers and for the 429-under-burst check.
    4. Guest CSP (the critical manual check): in a browser with DevTools console open, load a real guest SHARE link (`/share/<token>` or `/guest/share/<token>`) and a real guest SIGN link (`/sign/<token>` or `/guest/sign/<token>`). Confirm the PDF preview renders, any signature image renders, and there are ZERO `Content-Security-Policy` violation messages in the console. Repeat for the signature-verification page.
    5. Normal-traffic sanity: confirm a single normal request to a public route (and general authenticated app usage) is NOT throttled.
  </how-to-verify>
  <resume-signal>Type "approved" if all five headers, the 429 burst behavior, resource limits, and guest share/sign rendering all pass with no CSP console violations. Otherwise describe which check failed (e.g. which CSP directive blocked which resource) so plan 01-02 can be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → running edge | Operator runs verification tooling against the live nginx chokepoint |
| guest browser → CSP-governed page | The check confirms the CSP does not break legitimate same-origin PDF/signature rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Denial of Service (broken guest UX) | CSP vs guest share/sign rendering | mitigate | Blocking human-verify of guest PDF-preview + signature-image rendering with the console open; failure routes back to a CSP fix in 01-02 rather than accepting a broken flow |
| T-01-11 | Repudiation / verification gap | header + rate-limit claims | mitigate | Reusable smoke-check script gives a repeatable, non-interactive assertion of the header and 429 behavior on every future deploy |
| T-01-12 | Tampering | verification script / no package installs | accept | Pure bash + curl; no npm/pip/cargo install introduced |
</threat_model>

<verification>
- `edge-smoke-check.sh` passes for all five headers and the 429 burst check against the running host.
- `docker compose config` shows limits on every service.
- Operator confirms guest share, guest sign, and signature-verification pages render with no CSP console violations and no unexpected throttling.
</verification>

<success_criteria>
- Phase-level behavioral success criteria confirmed at runtime: headers present (AUTHSEC-03), rate limiting active (SEC-01), resource limits applied (INFRA-01), and guest flows unbroken.
- A reusable smoke-check script is committed for future regression checks.
</success_criteria>

<output>
Create `.planning/phases/01-deploy-edge-hardening/01-03-SUMMARY.md` when done.
</output>
