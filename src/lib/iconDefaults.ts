/** Tamanhos padrão Material Symbols — sidebar/inline, badge/botão pequeno. */
export const ICON_SIZE = {
  nav: 20,
  md: 20,
  sm: 18,
  xs: 16,
} as const;

/** Classe Tailwind para botões de ação do header de workspace. */
export const WORKSPACE_ACTION_BTN = 'workspace-action-btn';
